<?xml version="1.0" encoding="utf-8"?>
<search> 
  
    
    <entry>
      <title><![CDATA[Everything_Achievements]]></title>
      <url>/2019/12/22/Everything-Achievements/</url>
      <content type="html"><![CDATA[<p>See the Help section when you press Esc in-game for information on how to do specific actions. (If the information about a particular action is not there it means you haven’t unlocked it yet, keep playing).<br>If you know a more exact way of triggering an Achievement than it is written here, feel free to write it in the comments down below.</p>
<h2><a href="#action-achievements" class="header-anchor"># </a><span id="action-achievements">Action Achievements</span></h2><p>Action Achievements can be obtained by doing specific actions in the game.</p>
<p><strong>Thought Collector</strong>(Everything listens closely to itself) - Collect x Thoughts. (Press x while being near a Thing that has a speech bubble icon to collect a Thought)</p>
<p><strong>Communicator</strong>(Everything hears itself sing) - Sing while being near other Things of the same kind. (Press spacebar to Sing)</p>
<p><strong>A Merry Band</strong>(Everything gets itself mixed up) - Join with other Things. (Press v while being near other Things of the same kind to make them Join you, can be switched to the same category or everything when pressing q)</p>
<p><strong>The Swing Of Things</strong>(Everything sees itself through itself) - See your Thing in the world.</p>
<p><strong>Bonder</strong>(Everything sees itself through everything) - Bond x times. (Hold the first mouse button to Bond into a smaller Thing or the second mouse button to Bond into a bigger Thing)</p>
<p><strong>Descender</strong>(Everything is the same all the way down) - Keep Descending until you reach the Subatomic level. (To Descend you need to be a small Thing in your current place, then a triangle will appear when you hold the first mouse button)</p>
<p><strong>Symmetrizer</strong>(Everything watches itself dance) - Dance for 10? minutes, then leave the dance by moving. (Press g while Joined with at least 1 other Thing to start Dancing) (Press v while being near other Things of the same kind to make them Join you, can be switched to the same category or everything when pressing q)</p>
<p><strong>Thinker</strong>(Everything thinks about itself) - Think x times. (Press x when the ball/eye at the top starts glowing to Think)</p>
<p><strong>Becoming</strong>(Everything has the ability to transform itself) - Transform 1 time. (Hold e to Transform)</p>
<p><strong>No Such Things</strong>(Everything transforms itself over and over and over) - Transform x times. (Hold e to Transform)</p>
<p><strong>Nested Worlds</strong>(Everything has no dimensions) - Transform into a Planet, then Descend into it. (Hold e to Transform) (Keep Ascending until the Planetary level to unlock Planets)</p>
<p><strong>This Is It</strong>(Everything renews itself) - Clear x Thoughts. (Press m to open your Thoughts, then hold c to clear them) (Press x while being near a Thing that has a speech bubble icon to collect a Thought)</p>
<p><strong>You Are Nothing</strong>(Everything is Nothing) - Collect all 66 audio clips. (Press x while being near a Thing that has a circle icon to listen to an audio clip and collect it) (Audio clips may repeat themselves in the world)</p>
<h2><a href="#unlock-achievements" class="header-anchor"># </a><span id="unlock-achievements">Unlock Achievements</span></h2><p>Unlock Achievements can be obtained by unlocking Things in the game, to unlock Things you need to Bond with them once. (Hold the first mouse button to Bond into a smaller Thing or the second mouse button to Bond into a bigger Thing)</p>
<p><strong>Categoricalist</strong>(Everything opens itself wider and wider) - Unlock x categories of Things.</p>
<p><strong>Structuralist</strong>(Everything finds itself in Structures) - Unlock over 50% of Things in the Building category.</p>
<p><strong>Cold Blood</strong>(Everything finds itself in Reptiles) - Unlock over 50% of Things in the Reptile category.</p>
<h2><a href="#other-achievements" class="header-anchor"># </a><span id="other-achievements">Other Achievements</span></h2><p>Other Achievements that can be obtained in the game.</p>
<p><strong>Welcome To Everything</strong> (Everything finally begins) - Finish the tutorial. (It’s quite a lengthy one)</p>
<p><strong>Destroyer Of Worlds</strong> (Everything destroys itself) - Obtain 30~50 Disasters. (Disasters will appear in the world when your PC can’t handle the game/the game has low FPS, Disasters appear as random things/words appearing on the screen with loud sounds. Best way to achieve Disasters is to add a lot of Things that move around and enlarge all of them while Joined in the Galactic level)</p>
<p><strong>Everything is Everything</strong> (Everything is Everything) - Unlock all other Achievements.</p>
]]></content>
      
        
        <tags>
            
            <tag> Everything 游戏 </tag>
            
        </tags>
        
    </entry>
    
    <entry>
      <title><![CDATA[简单的链表及二叉树学习]]></title>
      <url>/2019/11/29/%E7%AE%80%E5%8D%95%E7%9A%84%E9%93%BE%E8%A1%A8%E5%8F%8A%E4%BA%8C%E5%8F%89%E6%A0%91%E5%AD%A6%E4%B9%A0/</url>
      <content type="html"><![CDATA[<p>本节课将会讲三点内容，链表，栈以及二叉树。</p>
<a id="more"></a>

<div class="toc">

<!-- toc -->

<ul>
<li><a href="#lian-biao">链表</a><ul>
<li><a href="#yue-se-fu-wen-ti">约瑟夫问题</a></li>
<li><a href="#jie-ti-si-lu">解题思路</a><ul>
<li><a href="#di-yi-chong-si-lu-shu-zu-biao-ji">第一种思路（数组标记）</a></li>
<li><a href="#di-er-chong-si-lu-suo-yin-shu-zu">第二种思路（索引数组）</a></li>
<li><a href="#di-san-chong-si-lu-suo-yin-lian-biao">第三种思路（索引链表）</a></li>
<li><a href="#di-si-chong-si-lu-lian-biao">第四种思路（链表）</a></li>
</ul>
</li>
<li><a href="#shi-me-shi-lian-biao">什么是链表</a></li>
<li><a href="#lian-biao-he-shu-zu-dui-bi">链表和数组对比</a></li>
<li><a href="#lian-biao-de-fen-lei">链表的分类</a></li>
<li><a href="#lian-biao-in-stl">链表 in STL</a></li>
</ul>
</li>
<li><a href="#zhan">栈</a><ul>
<li><a href="#hou-zhui-biao-da-shi">后缀表达式</a></li>
<li><a href="#jie-ti-si-lu-1">解题思路</a></li>
<li><a href="#shi-yong-zhan-lai-jie-ti">使用栈来解题</a></li>
<li><a href="#zhan-de-ding-yi">栈的定义</a></li>
<li><a href="#zhan-de-chang-jian-shi-xian">栈的常见实现</a></li>
<li><a href="#ti-ji-dui-lie">提及队列</a></li>
<li><a href="#xian-xing-shu-ju-jie-gou-dui-bi">线性数据结构对比</a></li>
</ul>
</li>
<li><a href="#er-cha-shu">二叉树</a><ul>
<li><a href="#hou-zhui-biao-da-shi-zhuan-zhong-zhui-biao-da-shi">后缀表达式转中缀表达式</a></li>
<li><a href="#jie-ti-si-lu-2">解题思路</a></li>
<li><a href="#er-cha-shu-ding-yi">二叉树定义</a></li>
<li><a href="#zen-me-jie-ti">怎么解题？</a></li>
<li><a href="#er-cha-shu-bian-li">二叉树遍历</a></li>
</ul>
</li>
</ul>
<!-- tocstop -->

</div>

<h2><a href="#lian-biao" class="header-anchor"># </a><span id="lian-biao">链表</span></h2><p>先来看一个简单的题目：</p>
<h3><a href="#yue-se-fu-wen-ti" class="header-anchor"># </a><span id="yue-se-fu-wen-ti">约瑟夫问题</span></h3><p> n个人围成一圈，从第一个人开始报数，数到m的人出列，再由下一个人重新从1开始报数，数到m的人再出圈，……依次类推，直到所有的人都出圈，请输出依次出圈人的编号。</p>
<p><strong>输入</strong></p>
<p>一行有两个数字，n和m，如题意。</p>
<p><strong>输出</strong></p>
<p>一行，为所有出圈人的编号。</p>
<p><strong>样例</strong></p>
<table>
<thead>
<tr>
<th align="left">输入</th>
<th align="left">输出</th>
</tr>
</thead>
<tbody><tr>
<td align="left">10 3</td>
<td align="left">3 6 9 2 7 1 8 5 10 4</td>
</tr>
</tbody></table>
<p><strong>数据范围</strong></p>
<p>1 &lt;= m &lt;= n &lt;= 100</p>
<h3><a href="#jie-ti-si-lu" class="header-anchor"># </a><span id="jie-ti-si-lu">解题思路</span></h3><p>约瑟夫问题是一个典型的模拟问题。</p>
<h4><a href="#di-yi-chong-si-lu-shu-zu-biao-ji" class="header-anchor"># </a><span id="di-yi-chong-si-lu-shu-zu-biao-ji">第一种思路（数组标记）</span></h4><p>我们可以创建一个数组，数组长度不小于总人数，每当一个人出列时，在数组内做一个标记。</p>
<pre><code class="cpp">int a[105];

// ...

int num=n,last=1;
    while(num){
        int cnt=0;
        for(int i=last;cnt!=m;i++){
            if(i&gt;n) i%=n;
            if(a[i]==-1) continue;
            ++cnt;
            if(cnt==m){
                a[i]=-1;
                num--;
                last=i+1;
                printf(&quot;%d &quot;,i);
                break;
            }
        }
    }</code></pre>
<h4><a href="#di-er-chong-si-lu-suo-yin-shu-zu" class="header-anchor"># </a><span id="di-er-chong-si-lu-suo-yin-shu-zu">第二种思路（索引数组）</span></h4><p>我们需要一种存储方式，能够在修改后快速找到下一个有效位置，而不是顺序检查。</p>
<p>不妨采用这样的方式：</p>
<pre><code class="cpp">int prev[105], next[105];

// ...

for(int i=1; i&lt;=n; ++i)
{
    prev[i] = ((i-1)-1+n)%n+1;
    next[i] = ((i-1)+1  )%n+1;
}

// ...

// 去掉 下标 i 的人
next[prev[i]] = next[i];
prev[next[i]] = prev[i];</code></pre>
<h4><a href="#di-san-chong-si-lu-suo-yin-lian-biao" class="header-anchor"># </a><span id="di-san-chong-si-lu-suo-yin-lian-biao">第三种思路（索引链表）</span></h4><p>上面的 prev 和 next 是分离的，有时候进行一些处理时可能不够直观，因此我们采用数据结构——链表。</p>
<p>先定义一个整合了 prev 和 next 的结构体：</p>
<pre><code class="cpp">struct Node
{
    int prev;
    int next;
};</code></pre>
<p>如果是 C，为了便于编程，在下面再加一行</p>
<pre><code class="c">typedef struct Node Node;</code></pre>
<p>在使用的时候，采用这种方式</p>
<pre><code class="cpp">Node a[105];

// ...

// 去掉 下标 i 的人
a[a[i].prev].next = a[i].next;
a[a[i].next].prev = a[i].prev;</code></pre>
<p>这种方式的使用有一些限制，其中最显然的一点是，a，也即是存储这些 Node 的对象，必须是可以使用整数作为索引的。</p>
<p>对于数组（和上节课描述过的 <code>std::vector</code> 、 <code>template&lt;typename T&gt; std::map&lt;int, T&gt;</code> 以及<code>std::set&lt;int&gt;</code> ）来说，这一假设可以满足，但是对于更多的情况，这一假设可能会限制我们的使用。</p>
<p>其次，这种方式使用时，还暗示了，删除一个元素很容易，然而增加新元素却很难。</p>
<p>考虑到 c 和 cpp 中常见的获取新的存储空间的方式（c：<code>malloc</code> ，<code>realloc</code> 和 <code>calloc</code> ；cpp：<code>new</code> ），我们可以认为，指针是一种通用的存储方式，因此，如果能用指针表示 prev 和 next ，我们就可以避免被上面的限制所束缚。</p>
<h4><a href="#di-si-chong-si-lu-lian-biao" class="header-anchor"># </a><span id="di-si-chong-si-lu-lian-biao">第四种思路（链表）</span></h4><p>在这里，我们选择使用指针作为索引。</p>
<pre><code class="cpp">struct Node
{
    int id;
    Node *prev;
    Node *next;
};</code></pre>
<p>如果是 C 语言，请在最前面加上 <code>typedef struct Node Node;</code></p>
<p>去掉某个人，则采用这样的方法：</p>
<pre><code class="cpp">void remove(Node *p)
{
    if(p-&gt;prev &amp;&amp; p-&gt;next)
    {
        p-&gt;prev-&gt;next = p-&gt;next;
        p-&gt;next-&gt;prev = p-&gt;prev;
        delete p;
        // if in c, use free
    }
}</code></pre>
<p>最后一种方式的优点在于，我们不需要事先准备一个足够大的数组，而是可以根据输入数据动态创建一个足够长的链。</p>
<h3><a href="#shi-me-shi-lian-biao" class="header-anchor"># </a><span id="shi-me-shi-lian-biao">什么是链表</span></h3><p>链表是一种物理存储单元上非连续、非顺序的存储结构，数据元素的逻辑顺序是通过链表中的指针链接次序实现的。<br>链表由一系列结点（链表中每一个元素称为结点）组成，结点可以在运行时动态生成。<br>每个结点包括两个部分：一个是存储数据元素的数据域，另一个是存储下一个结点地址的指针域。 </p>
<p>例如，一个长度为 3 的链表可以是这样的。</p>
<pre class="mermaid">graph LR;
    A-->B;
    B-->C;
    A["A: 1 | &B"];
    B["B: 3 | &C"];
    C["C: 12 | NULL"];</pre>

<p>这个链表共有三个元素，A-C。其中 A 的数据域为 1，指针域为 &amp;B（B的地址）。最后的 C 数据域为 12，指针域为空（NULL）。可以使用一个循环来遍历这个链表：</p>
<pre><code class="cpp">struct Node
{
    int v;
    Node *next;
};

Node *head = GetHead();</code></pre>
<pre><code class="cpp">for(Node *p = head; p; p = p-&gt;next)
{
    printf(&quot;%d\n&quot;, p-&gt;v);
}</code></pre>
<h3><a href="#lian-biao-he-shu-zu-dui-bi" class="header-anchor"># </a><span id="lian-biao-he-shu-zu-dui-bi">链表和数组对比</span></h3><p>此处的数组指广义数组（含<code>std::array</code> 及<code>std::vector</code>）</p>
<table>
<thead>
<tr>
<th align="left">对比项</th>
<th align="left">链表</th>
<th>数组</th>
</tr>
</thead>
<tbody><tr>
<td align="left">查找第 n 个元素</td>
<td align="left">O(n)</td>
<td>O(1)</td>
</tr>
<tr>
<td align="left">删去第 n 个元素</td>
<td align="left">O(1)</td>
<td>O(n)</td>
</tr>
<tr>
<td align="left">插入元素到某位置</td>
<td align="left">O(1)</td>
<td>O(n)</td>
</tr>
<tr>
<td align="left">将某一段逆序</td>
<td align="left">O(n)</td>
<td>O(n)</td>
</tr>
</tbody></table>
<h3><a href="#lian-biao-de-fen-lei" class="header-anchor"># </a><span id="lian-biao-de-fen-lei">链表的分类</span></h3><ul>
<li>单链表<ul>
<li>数据域：数据；</li>
<li>指针域：next 指针，表示其下一个节点的地址</li>
</ul>
</li>
<li>循环链表<ul>
<li>单链表</li>
<li>最后一个节点的 next 为首个节点的地址</li>
</ul>
</li>
<li>双向链表<ul>
<li>指针域：next 指针和 prev 指针，分别表示下一个节点和上一个节点的地址。</li>
</ul>
</li>
</ul>
<h3><a href="#lian-biao-in-stl" class="header-anchor"># </a><span id="lian-biao-in-stl">链表 in STL</span></h3><p>STL 中也有链表类型的实现：std::list</p>
<p>其支持的方法如下：</p>
<table>
<thead>
<tr>
<th><strong>list成员</strong></th>
<th><strong>说明</strong></th>
</tr>
</thead>
<tbody><tr>
<td>operator=</td>
<td>赋值重载运算符</td>
</tr>
<tr>
<td>assign</td>
<td>分配值</td>
</tr>
<tr>
<td>front</td>
<td>返回第一个元素的引用</td>
</tr>
<tr>
<td>back</td>
<td>返回最后一元素的引用</td>
</tr>
<tr>
<td>begin</td>
<td>返回第一个元素的指针(iterator)</td>
</tr>
<tr>
<td>end</td>
<td>返回最后一个元素的下一位置的指针</td>
</tr>
<tr>
<td>rbegin</td>
<td>返回链表最后一元素的后向指针(reverse_iterator or const)</td>
</tr>
<tr>
<td>rend</td>
<td>返回链表第一元素的下一位置的后向指针</td>
</tr>
<tr>
<td>push_back</td>
<td>增加一元素到链表尾</td>
</tr>
<tr>
<td>push_front</td>
<td>增加一元素到链表头</td>
</tr>
<tr>
<td>pop_back</td>
<td>pop_back()删除链表尾的一个元素</td>
</tr>
<tr>
<td>pop_front</td>
<td>删除链表头的一元素</td>
</tr>
<tr>
<td>clear</td>
<td>删除所有元素</td>
</tr>
<tr>
<td>erase</td>
<td>删除一个元素或一个区域的元素(两个重载)</td>
</tr>
<tr>
<td>remove</td>
<td>删除链表中匹配值的元素(匹配元素全部删除)</td>
</tr>
<tr>
<td>remove_if</td>
<td>删除条件满足的元素(遍历一次链表)，参数为自定义的回调函数</td>
</tr>
<tr>
<td>empty</td>
<td>判断是否链表为空</td>
</tr>
<tr>
<td>max_size</td>
<td>返回链表最大可能长度</td>
</tr>
<tr>
<td>size</td>
<td>返回链表中元素个数</td>
</tr>
<tr>
<td>resize</td>
<td>重新定义链表长度(两重载函数)</td>
</tr>
<tr>
<td>reverse</td>
<td>反转链表</td>
</tr>
<tr>
<td>sort</td>
<td>对链表排序，默认升序</td>
</tr>
<tr>
<td>merge</td>
<td>合并两个有序链表并使之有序</td>
</tr>
<tr>
<td>splice</td>
<td>对两个链表进行结合(三个重载函数) 结合后第二个链表清空</td>
</tr>
<tr>
<td>insert</td>
<td>在指定位置插入一个或多个元素(三个重载函数)</td>
</tr>
<tr>
<td>swap</td>
<td>交换两个链表(两个重载)</td>
</tr>
<tr>
<td>unique</td>
<td>删除相邻重复元素</td>
</tr>
</tbody></table>
<h2><a href="#zhan" class="header-anchor"># </a><span id="zhan">栈</span></h2><h3><a href="#hou-zhui-biao-da-shi" class="header-anchor"># </a><span id="hou-zhui-biao-da-shi">后缀表达式</span></h3><p>所谓后缀表达式是指这样的一个表达式：式中不再引用括号，运算符号放在两个运算对象之后，所有计算按运算符号出现的顺序，严格地由左而右新进行（不用考虑运算符的优先级）。</p>
<p>如：<code>3*(5–1)+7</code>对应的后缀表达式为：<code>3 5 1 - * 7 +</code>。</p>
<p><strong>输入</strong></p>
<p>一行，其中运算符和运算数之间都用空格分隔，运算数是一个一位非负整数。</p>
<p><strong>输出</strong></p>
<p> 一行，表达式的值。</p>
<p><strong>样例</strong></p>
<table>
<thead>
<tr>
<th align="left">输入</th>
<th align="left">输出</th>
</tr>
</thead>
<tbody><tr>
<td align="left">3 5 1 - * 7 +</td>
<td align="left">19</td>
</tr>
</tbody></table>
<p><strong>数据范围</strong></p>
<p>无。</p>
<h3><a href="#jie-ti-si-lu" class="header-anchor"># </a><span id="jie-ti-si-lu">解题思路</span></h3><p>这个问题看似麻烦，我们不妨先来手动模拟一次。</p>
<p>3 5 1 - * 7 +</p>
<p>首先参与运算的是 “5 1 -“ ，得到结果 4，将 4 填在原来 “5 1 -“ 的位置。</p>
<p>这时候，表达式为 3 4 * 7 +</p>
<p>我们接下来算乘法，得到结果 12</p>
<p>现在表达式为 12 7 +</p>
<p>两者相加，得到结果 19。</p>
<hr>
<p>通过观察我们的求解过程，我们可以发现，我们每次选择最靠前的一个符号，并将其前方的两个数字进行运算。</p>
<p>但是具体实现起来该如何做呢？</p>
<p><font size="5"><del>&gt;&gt;&gt;&gt;<strong>栈</strong>&lt;&lt;&lt;&lt;</del></font></p>
<p>结合前面的知识，我们发现，可以使用一个链表，这个链表结构如下：</p>
<pre><code class="cpp">struct Node
{
    int type;
    int v;
    Node *next, *prev;
};</code></pre>
<p>其中，type 表示其为数字还是为运算符。</p>
<p>如果是数字，v 表示 数字的值；如果为运算符，我们不妨用 0123 代指四种运算符。</p>
<p>接下来，每次从首个节点开始查找，找到第一个 <code>type == 运算符</code> 的节点，然后获得其前方的两个节点的值，运算后用其结果代替这三个节点。</p>
<p>等到最后，首元素的 next 为 NULL 的时候，即链表长度为 1 时，最后这个节点的 v 就是结果。</p>
<p><del> 所以跟栈有什么关系 </del></p>
<h3><a href="#shi-yong-zhan-lai-jie-ti" class="header-anchor"># </a><span id="shi-yong-zhan-lai-jie-ti">使用栈来解题</span></h3><p>观察我们的解题思路，我们可以发现这样一个特点。</p>
<p>我们每次处理的时候，都只对前面的一部分数据进行处理。</p>
<p>如下：</p>
<ol>
<li><p><font color="gree">3 </font><font color="blue">5 1 </font><font color="red">-</font> <font color="gray">* 7 +</font></p>
</li>
<li><p><font color="blue">3 4 </font><font color="red">*</font> <font color="gray">7 +</font></p>
</li>
<li><p><font color="blue">12 7 </font><font color="red">+</font></p>
</li>
<li><p><font color="blue">19</font></p>
</li>
</ol>
<p>其中，我们可以发现，绿色和灰色的部分是完全不用考虑的。</p>
<p>每次操作是找到红色的操作符，然后取出蓝色的数字，进行运算后放回对应位置。</p>
<p>如果我们不看灰色部分，我们可以得到这样的操作流程：</p>
<p>每次取出最后一个节点（操作符），然后再取出两个节点作为操作数，运算后将结果放回原位置。</p>
<p>我们归纳这个过程，可以抽象出这样的行为：</p>
<ol>
<li><p>取出元素：从一系列元素的最后面获得一个元素，元素数量减一；</p>
</li>
<li><p>放入元素：在一系列元素的最后面添加一个元素，元素数量加一。</p>
</li>
</ol>
<p>在栈中，我们将这两个操作称为 push 和 pop。push 是压入一个元素，而 pop 是弹出一个元素。</p>
<hr>
<p>通过整理上面的过程，我们可以得到这样的流程：<br>首先创建一个栈，然后读入数据，每当读入一个元素，就将一个元素压栈。（这里的元素包括数字和操作符）</p>
<p>如果刚刚压栈的是操作符，就弹出三个元素，按照出栈的顺序记为 a，b，c（其中，a 是刚刚压栈的操作符），并将 c (a) b 的结果压栈。</p>
<p>直到读入完数据后，栈中剩余一个元素，这个元素就是结果。</p>
<hr>
<p><strong>更加简洁</strong></p>
<p>上面的方法中，每次操作符被压栈后就立即出栈，而这一过程可以被简化：</p>
<p>输入数据：</p>
<p>如果数据为 数字 ，则压栈；</p>
<p>如果数据为 操作符，则弹出两个数字，并将运算结果压栈。</p>
<h3><a href="#zhan-de-ding-yi" class="header-anchor"># </a><span id="zhan-de-ding-yi">栈的定义</span></h3><p>栈（stack）又名堆栈，它是一种运算受限的线性表。<br>限定仅在表尾进行插入和删除操作的线性表。这一端被称为栈顶，相对地，把另一端称为栈底。<br>向一个栈插入新元素又称作进栈、入栈或压栈，它是把新元素放到栈顶元素的上面，使之成为新的栈顶元素；<br>从一个栈删除元素又称作出栈或退栈，它是把栈顶元素删除掉，使其相邻的元素成为新的栈顶元素。</p>
<p><strong>栈的特点可以概括为 FILO (First In Last Out)，即越先进去的元素，越后出来</strong></p>
<h3><a href="#zhan-de-chang-jian-shi-xian" class="header-anchor"># </a><span id="zhan-de-chang-jian-shi-xian">栈的常见实现</span></h3><ol>
<li>可以用数组和一个数字模拟栈</li>
<li>STL std::stack</li>
<li>用链表来实现栈。</li>
</ol>
<h3><a href="#ti-ji-dui-lie" class="header-anchor"># </a><span id="ti-ji-dui-lie">提及队列</span></h3><p>队列与栈相似，不同点在于其为先进先出。</p>
<h3><a href="#xian-xing-shu-ju-jie-gou-dui-bi" class="header-anchor"># </a><span id="xian-xing-shu-ju-jie-gou-dui-bi">线性数据结构对比</span></h3><table>
<thead>
<tr>
<th align="left"></th>
<th align="left">数组（广义）</th>
<th>链表</th>
<th>栈</th>
<th>队列</th>
</tr>
</thead>
<tbody><tr>
<td align="left">支持下标操作（随机寻址）</td>
<td align="left">是</td>
<td>否</td>
<td>否</td>
<td>否</td>
</tr>
<tr>
<td align="left">增删元素的复杂度</td>
<td align="left">O(n)</td>
<td>O(1)</td>
<td>/</td>
<td>/</td>
</tr>
<tr>
<td align="left">支持遍历所有元素</td>
<td align="left">是</td>
<td>是</td>
<td>否</td>
<td>否</td>
</tr>
<tr>
<td align="left">求指定位置元素的复杂度</td>
<td align="left">O(1)</td>
<td>O(n)</td>
<td>/</td>
<td>/</td>
</tr>
<tr>
<td align="left">在末端增加新元素复杂度</td>
<td align="left">O(1)</td>
<td>O(1)</td>
<td>O(1)</td>
<td>O(1)</td>
</tr>
<tr>
<td align="left">能访问的元素数量</td>
<td align="left">所有</td>
<td>所有</td>
<td>仅栈顶</td>
<td>仅队首</td>
</tr>
<tr>
<td align="left">能操作的位点数量</td>
<td align="left">所有</td>
<td>所有</td>
<td>仅栈顶</td>
<td>队首和队尾</td>
</tr>
</tbody></table>
<pre class="mermaid">graph TB;
    A[数组]-->C[栈];
    A-->B;
    B[链表]-->C;
    A-->D[队列];
    B-->D;</pre>

<h2><a href="#er-cha-shu" class="header-anchor"># </a><span id="er-cha-shu">二叉树</span></h2><h3><a href="#hou-zhui-biao-da-shi-zhuan-zhong-zhui-biao-da-shi" class="header-anchor"># </a><span id="hou-zhui-biao-da-shi-zhuan-zhong-zhui-biao-da-shi">后缀表达式转中缀表达式</span></h3><p>后缀表达式定义如前，请根据给定后缀表达式，采用完全加括号的形式，输出其确定的中缀表达式。</p>
<p>如：<code>3*(5–1)+7</code>对应的后缀表达式为：<code>3 5 1 - * 7 +</code>，我们对其完全加括号，可以得到<code>((3*(5-1))+7)</code> 。</p>
<p><strong>输入</strong></p>
<p>一行，其中运算符和运算数之间都用空格分隔，运算数是一个一位非负整数。</p>
<p><strong>输出</strong></p>
<p> 一行，中缀表达式。</p>
<p><strong>样例</strong></p>
<table>
<thead>
<tr>
<th align="left">输入</th>
<th align="left">输出</th>
</tr>
</thead>
<tbody><tr>
<td align="left">3 5 1 - * 7 +</td>
<td align="left">((3*(5-1))+7)</td>
</tr>
</tbody></table>
<p><strong>数据范围</strong></p>
<p>无。</p>
<h3><a href="#jie-ti-si-lu" class="header-anchor"># </a><span id="jie-ti-si-lu">解题思路</span></h3><p>针对样例，我们可以发现，有这样的层级结构</p>
<pre class="mermaid">graph TB;
    A["((3*(5-1))+7)"]-->B["(3*(5-1))"];
    A-->C["7"];
    B-->D["3"];
    B-->E["(5-1)"];
    E-->F["5"];
    E-->G["1"];</pre>

<p>每个表达式可以被抽象为 <code>(a op b)</code> 的形式，其中，a、b 为表达式，op 为操作符。</p>
<p>表达式有两种形式：<code>(a op b)</code> 或者 <code>a</code> ，其中 a、b 为表达式，op 为操作符。</p>
<p>也就是说，表达式可以是单个数字。</p>
<p>通过看图，我们可以发现，3、5、1、7 这四个节点，也就是操作数节点，没有下属节点（后称“子节点”）。<br>而当其有子节点的时候，如 <code>(5-1)</code> ，有两个操作数节点，因此有一层括号。</p>
<p>我们将其所有子节点的层数，取其中的最大值，称之为该节点的深度。<br>例如，<code>(5-1)</code> 深度为 1，<code>3</code> 深度为 0，而最终结果的深度为 <code>max(3, 1) = 3</code>。<br>对于深度为 d 的节点，其有 d 层括号。</p>
<p>因为这个层级结构像一棵倒置的树，因此这一结构被称为“树”。最上面的节点被称为根节点，而没有子节点的节点被称为叶节点。</p>
<pre class="mermaid">graph TB;
    A["根节点 ((3*(5-1))+7)"]-->B["(3*(5-1))"];
    A-->C["叶节点 7"];
    B-->D["叶节点 3"];
    B-->E["(5-1)"];
    E-->F["叶节点 5"];
    E-->G["叶节点 1"];</pre>

<p>也就是说，其结构如上所示。</p>
<p>对于每一组这样结构的节点</p>
<pre class="mermaid">graph TB;
    A["亲节点"]-->B["左子节点"];
    A-->C["右子节点"];</pre>

<p>其中 A 被称为亲节点（或父节点），而 B、C 则称之为子节点。</p>
<p>亲节点和子节点的定义的相对的，没有绝对的亲节点，也没有绝对的子节点。<br>特别的，仅当节点数量为 1 时，树中不存在亲节点与子节点的关系。</p>
<p>像表达式树这种，每个节点（最多）有两个子节点的情况，我们分别将这两个子节点称为左子节点和右子节点。而这个树称之为二叉树。</p>
<h3><a href="#er-cha-shu-ding-yi" class="header-anchor"># </a><span id="er-cha-shu-ding-yi">二叉树定义</span></h3><p>在计算机科学中，二叉树是每个结点最多有两个子树的树结构。通常子树被称作“左子树”（left subtree）和“右子树”（right subtree）。二叉树常被用于实现二叉查找树和二叉堆。</p>
<p>一棵深度为k，且有2^k-1个结点的二叉树，称为满二叉树。这种树的特点是每一层上的结点数都是最大结点数。而在一棵二叉树中，除最后一层外，若其余层都是满的，并且或者最后一层是满的，或者是在右边缺少连续若干结点，则此二叉树为完全二叉树。具有n个结点的完全二叉树的深度为floor(log2n)+1。深度为k的完全二叉树，至少有2k-1个叶子结点，至多有2k-1个结点。</p>
<h3><a href="#zen-me-jie-ti" class="header-anchor"># </a><span id="zen-me-jie-ti">怎么解题？</span></h3><p>对于上面的题目，我们可以这样做：</p>
<p>将栈（后缀表达式）部分的计算过程进行修改。</p>
<p><strong>原来的过程是：</strong></p>
<p>输入一个元素；</p>
<p>如果是数字则压栈；</p>
<p>如果是操作符则出栈两个数字，运算后压栈。</p>
<p><strong>在这里，我们修改过程：</strong></p>
<p>输入一个元素；</p>
<p>如果是数字，则压栈一个值为此数字的节点；</p>
<p>如果是操作符，则出栈两个节点，生成新的节点后压栈。</p>
<p><strong>表达式的保存</strong></p>
<p>我们可以采用这样的结构：</p>
<pre><code class="cpp">struct Node
{
    bool isNum;
    int val;
    Node *left, *right;
};</code></pre>
<p>这样一来，上面的树就可以如下表示：</p>
<pre class="mermaid">graph TB;
    A["A: false | '+' | &B | &C"]-->B["B: false | '*' | &D | &E"];
    A-->C["C: true | 7 | NULL | NULL"];
    B-->D["D: true | 3 | NULL | NULL"];
    B-->E["E: false | '-' | &F | &G"];
    E-->F["F: true | 5 | NULL | NULL"];
    E-->G["G: true | 1 | NULL | NULL"];</pre>

<p>通过观察我们可以发现，所有叶节点都是操作数，非叶节点都是操作符。</p>
<p>因此我们可以使用这样的结构：</p>
<pre><code class="cpp">struct Node
{
    int val;
    Node *left, *right;
};</code></pre>
<pre class="mermaid">graph TB;
    A["A: '+' | &B | &C"]-->B["B: '*' | &D | &E"];
    A-->C["C: 7 | NULL | NULL"];
    B-->D["D: 3 | NULL | NULL"];
    B-->E["E: '-' | &F | &G"];
    E-->F["F: 5 | NULL | NULL"];
    E-->G["G: 1 | NULL | NULL"];</pre>

<p>而要判断一个节点是操作数还是操作符，可以如下判断：</p>
<pre><code class="cpp">bool isNum(Node *p)
{
    return p-&gt;left == NULL &amp;&amp; p-&gt;right == NULL;
}</code></pre>
<p>在生成这样一个树形结构后，我们就可以进行输出。</p>
<h3><a href="#er-cha-shu-bian-li" class="header-anchor"># </a><span id="er-cha-shu-bian-li">二叉树遍历</span></h3><blockquote>
<p>遍历是对树的一种最基本的运算，所谓遍历二叉树，就是按一定的规则和顺序走遍二叉树的所有结点，使每一个结点都被访问一次，而且只被访问一次。由于二叉树是非线性结构，因此，树的遍历实质上是将二叉树的各个结点转换成为一个线性序列来表示。</p>
<p>设L、D、R分别表示遍历左子树、访问根结点和遍历右子树， 则对一棵二叉树的遍历有三种情况：DLR（称为先根次序遍历，也叫先序遍历），LDR（称为中根次序遍历，也叫中序遍历），LRD （称为后根次序遍历，也叫后序遍历）。</p>
</blockquote>
<p>要输出表达式，我们采用中序遍历的方式。</p>
<p>对于每个节点，如果其为表达式，则遍历输出，如果为数字，则直接输出。</p>
]]></content>
      
        
        <tags>
            
            <tag> 社团课程 零基础课程 </tag>
            
        </tags>
        
    </entry>
    
    <entry>
      <title><![CDATA[面向对象入门]]></title>
      <url>/2019/11/11/OO-Basic-basedoncpp/</url>
      <content type="html"><![CDATA[<blockquote><p>If you think C++ is not overly complicated, just what is a protected abstract virtual base pure virtual private destructor and when was the last time you needed one?</p>
<footer><strong>[Tom Cargill]</strong></footer></blockquote>

<a id="more"></a>

<p>本文通篇将会构造一个类，并以此类的构造过程为讲解顺序。</p>
<p>本文需求的先修知识：cpp <em>iostream</em> (<code>std::cin,std::cout</code> &amp; <code>std::endl</code>)，cpp <em>class defination</em>, c <em>struct</em>, c <em>pointer</em> and cpp <em>keywords</em> (<code>const, new, delete, class, public, private</code> [<code>protected, pure, virtual</code>])</p>
<div class="toc">

<!-- toc -->

<ul>
<li><a href="#bian-cheng-mu-biao">编程目标</a></li>
<li><a href="#mian-xiang-guo-cheng-ke-yi-ma">面向过程可以吗？</a></li>
<li><a href="#c-yu-yan-ye-neng-mian-xiang-dui-xiang-ma">C 语言也能面向对象吗？</a><ul>
<li><a href="#c-yu-yan-mian-xiang-dui-xiang">C 语言面向对象</a></li>
<li><a href="#you-he-que-xian">有何缺陷？</a></li>
<li><a href="#c-yu-yan-ke-hou-xi-ti">C语言课后习题</a></li>
</ul>
</li>
<li><a href="#cpp-mian-xiang-dui-xiang">Cpp 面向对象</a><ul>
<li><a href="#gou-zao-he-shu-chu-gong-neng">构造和输出功能</a></li>
<li><a href="#guan-li-nei-cun">管理内存</a></li>
<li><a href="#zen-me-zong-xiang-lian-jie-fu-kan-ji-cheng-guan-xi">怎么纵向连接？俯瞰继承关系</a><ul>
<li><a href="#wo-men-ying-gai-zen-yang-she-ji-ni">我们应该怎样设计呢？</a></li>
<li><a href="#wo-men-xu-yao-na-xie-lei-xing-ni">我们需要哪些类型呢？</a></li>
<li><a href="#wo-men-zen-yang-guan-li-nei-cun">我们怎样管理内存？</a></li>
</ul>
</li>
<li><a href="#lei-xing-de-she-ji">类型的设计</a><ul>
<li><a href="#picture-lei">Picture 类</a></li>
<li><a href="#pictureentity-ji-lei">PictureEntity 基类</a></li>
<li><a href="#basicpicture">BasicPicture</a></li>
<li><a href="#framedpicture">FramedPicture</a></li>
<li><a href="#vcatpicture">VcatPicture</a></li>
<li><a href="#hcatpicture">HcatPicture</a></li>
</ul>
</li>
</ul>
</li>
<li><a href="#zong-shu-ji-xiao-guo-zhan-shi">总述及效果展示</a><ul>
<li><a href="#cpp-ke-hou-xi-ti">Cpp 课后习题</a></li>
</ul>
</li>
</ul>
<!-- tocstop -->

</div>

<hr>
<h2><a href="#bian-cheng-mu-biao" class="header-anchor"># </a><span id="bian-cheng-mu-biao">编程目标</span></h2><p>在命令行窗口编程时，我们有时候会希望输出一些特定的格式，如：（每行前面有一个空格）</p>
<pre><code> Hello,
 World!</code></pre><p>像这样的，基于字符构造的，有特定格式、内容的整体，我们姑且称之为 “字符图像”。<br>我们可以给字符图像加上边框：</p>
<pre><code>.-------.
| Hello,|
| World!|
&#39;-------&#39;</code></pre><p>更多地，可能会希望支持两个这样的字符图拼接，形成新的图像。</p>
<pre><code>.-------.
| Hello,| 你好，
| World!| 世界！
&#39;-------&#39;</code></pre><p>以及希望能够修改边框的样式等等······</p>
<hr>
<h2><a href="#mian-xiang-guo-cheng-ke-yi-ma" class="header-anchor"># </a><span id="mian-xiang-guo-cheng-ke-yi-ma">面向过程可以吗？</span></h2><p>如我们所学过的 C/Cpp， 我们可能会想到这样做：</p>
<pre><code class="cpp">int main()
{
    const char *content[] = {&quot;Hello,&quot;, &quot;World!&quot;};
    int left_padding = 1;
    for(int i=0; i&lt;2; ++i)
    {
        for(int j=0; j&lt; left_padding; ++j) printf(&quot; &quot;);
        printf(&quot;%s&quot;, content[i]);
        printf(&quot;\n&quot;);
    }
}</code></pre>
<p>运行可以得到输出：</p>
<pre><code> Hello, 
 World!</code></pre><p>但是很显然，这样无法（很难）对多个字符图进行操作。</p>
<h2><a href="#c-yu-yan-ye-neng-mian-xiang-dui-xiang-ma" class="header-anchor"># </a><span id="c-yu-yan-ye-neng-mian-xiang-dui-xiang-ma">C 语言也能面向对象吗？</span></h2><p>我们需要将一个图视为一个对象，一个物体，才能方便地管理、输出两个（及以上）的图。</p>
<h3><a href="#c-yu-yan-mian-xiang-dui-xiang" class="header-anchor"># </a><span id="c-yu-yan-mian-xiang-dui-xiang">C 语言面向对象</span></h3><p><em>如果下面的例程编译错误，请修改文件后缀为 cpp 或添加编译选项 -std=c99 或 -std=c11</em></p>
<p>我们用 C 语言来试试看：</p>
<pre><code class="c">struct tagPicture
{
    char **m_content;
    int m_padding_left, m_padding_top;
    int m_height, m_width;
};

typedef struct tagPicture Picture;</code></pre>
<p>在这个结构体 <code>struct tagPicture</code> （或记作<code>Picture</code>）中，我们使用 <code>m_content</code> 来保存图像内容，<code>m_height, m_width</code> 表示图像的宽与高，<code>m_padding_left</code> 和<code>m_padding_top</code> 表示图像左侧和上侧内边界大小。</p>
<p>怎么创建一个 <code>Picture</code> 呢？<br>在 C 语言中，我们需要定义一个函数来创建该类型：</p>
<pre><code class="c">// 此处需要库文件 stdlib.h 和 string.h
Picture getPicture(const char **_ArrayOfCharArray, int _Lines)
{
    Picture t;
    t.m_content = (char **) malloc(_Lines *sizeof(char *));
    t.m_padding_left = t.m_padding_top = 0;
    t.m_height = _Lines;
    t.m_width = 0;
    for(int i=0; i&lt;_Lines; ++i)
    {
        int n = _ArrayOfCharArray[i] ? strlen(_ArrayOfCharArray[i]) : 0;
        if( t.m_width &lt; n ) t.m_width = n; 
    }
    for(int i=0; i&lt;_Lines; ++i)
    {
        t.m_content[i] = (char *) malloc((t.m_width+1) * sizeof(char));
        strcpy(t.m_content[i], _ArrayOfCharArray[i]);
    }
    return t;
}</code></pre>
<p>还需要设定一个输出用的函数：</p>
<pre><code class="c">// 此处需要库文件 stdio.h
void printPicture(Picture _pic)
{
    for(int i=0; i&lt;_pic.m_height; ++i)
    {
        if(!(i &lt; _pic.m_padding_top))
        {
            for(int j=0; j&lt;_pic.m_width; ++j)
            {
                if(j &lt; _pic.m_padding_left) printf(&quot; &quot;);
                else if(_pic.m_content[j-_pic.m_padding_left])
                    printf(&quot;%c&quot;, _pic.m_content[i-_pic.m_padding_top][j-_pic.m_padding_left]);
                else break;
            }
        }
        if(i+1 != _pic.m_height) printf(&quot;\n&quot;);
    }
}</code></pre>
<p>接下来在主函数调用试试：</p>
<pre><code class="c">int main()
{
    const char *content[] = {&quot;Hello,&quot;, &quot;World!&quot;};
    Picture a = getPicture(content, 2);
//  a.m_padding_left = 1;
    printPicture(a);
}</code></pre>
<p>编译运行后可以得到这样的输出：</p>
<pre><code>Hello,
World!</code></pre><p>我们在主函数中将 <code>a.m_padding_left</code> 设置为 1，则会看到：</p>
<pre><code> Hello
 World</code></pre><h3><a href="#you-he-que-xian" class="header-anchor"># </a><span id="you-he-que-xian">有何缺陷？</span></h3><p>首先，我们的这个图像单个输出、存储都没有问题，但是如果想要拼接，尤其是横向拼接，就很难做，其次，进行其他复杂操作时，不能较容易地存储结构信息。</p>
<h3><a href="#c-yu-yan-ke-hou-xi-ti" class="header-anchor"># </a><span id="c-yu-yan-ke-hou-xi-ti">C语言课后习题</span></h3><ul>
<li>课后习题 1 : 经过上面的示例，我们可以发现纵向拼接很容易，在此处不做详解，将 C 语言面向对象实现图像的纵向拼接这一任务留作习题。</li>
<li>课后习题 2 : 纵向拼接实现很轻松，横向拼接相对来说难度略大，但聪明的读者一定能解决这个问题，请用 C 语言面向对象实现图像的横向拼接。</li>
<li>课后习题 3 : 请用 C 语言面向对象实现给图像加边框，边框样式需要作为函数参数，如可以指定用 ‘*‘ 来画边框。</li>
<li>课后习题 4 : 请用 C 语言面向对象实现图像修改边框样式，例如将 ‘*’ 样式的边框改为 ‘+’ 样式。</li>
</ul>
<h2><a href="#cpp-mian-xiang-dui-xiang" class="header-anchor"># </a><span id="cpp-mian-xiang-dui-xiang">Cpp 面向对象</span></h2><p>Cpp 的 class、struct 与 C 中的 struct 相似。</p>
<table>
<thead>
<tr>
<th align="center"></th>
<th align="center">C struct</th>
<th align="center">Cpp struct</th>
<th align="center">Cpp class</th>
</tr>
</thead>
<tbody><tr>
<td align="center">Plain Old Data</td>
<td align="center">true</td>
<td align="center">false</td>
<td align="center">false</td>
</tr>
<tr>
<td align="center">default access rights</td>
<td align="center">/</td>
<td align="center">public</td>
<td align="center">private</td>
</tr>
<tr>
<td align="center">member function enable</td>
<td align="center">false</td>
<td align="center">true</td>
<td align="center">true</td>
</tr>
</tbody></table>
<p>简单用法在此不做赘述。<br>不妨先实现上面程序的内容，让我们看看能否更加优雅。</p>
<h3><a href="#gou-zao-he-shu-chu-gong-neng" class="header-anchor"># </a><span id="gou-zao-he-shu-chu-gong-neng">构造和输出功能</span></h3><p>我们姑且以此结构构造程序：</p>
<pre><code>src
 |-- main.cpp
 |-- Picture.h
 &#39;-- Picture.cpp</code></pre><p>首先得定义这个类，并声明其方法。</p>
<pre><code class="cpp">#ifndef __PICTURE__INC
#define __PICTURE__INC

class Picture
{
public:
    Picture(const char **_Strs, int _Lines);
    void print(void);
private:
    char **m_content;
    int m_width, m_height;
    int m_padding_left, m_padding_top;
};

#endif</code></pre>
<p>接下来我们在主函数内直接定义 Picture 的实例 pic，并以此初始化字符串数组初始化这个实例。</p>
<p>此后，我们调用其输出方法。</p>
<pre><code class="cpp">//main.cpp
#include &quot;picture.h&quot;

const char *initstrs[] = {&quot;Hello,&quot;, &quot;World!&quot;};

int main()
{
    Picture pic(initstrs, 2);
    pic.print();
}</code></pre>
<p>最后，我们实现这两个函数。</p>
<pre><code class="cpp">// picture.cpp
#include &quot;picture.h&quot;
#include &lt;cstdio&gt;
#include &lt;cstdlib&gt;
#include &lt;cstring&gt;


Picture::Picture(const char **_Strs, int _Lines)
{
    m_content = (char **) malloc(_Lines *sizeof(char *));
    m_padding_left = m_padding_top = 0;
    m_height = _Lines;
    m_width = 0;
    for(int i=0; i&lt;_Lines; ++i)
    {
        int n = _Strs[i] ? strlen(_Strs[i]) : 0;
        if( m_width &lt; n ) m_width = n; 
    }
    for(int i=0; i&lt;_Lines; ++i)
    {
        m_content[i] = (char *) malloc((m_width+1) * sizeof(char));
        strcpy(m_content[i], _Strs[i]);
    }
}

void Picture::print(void)
{
    for(int i=0; i&lt;m_height; ++i)
    {
        if(!(i &lt; m_padding_top))
        {
            for(int j=0; j&lt;m_width; ++j)
            {
                if(j &lt; m_padding_left) printf(&quot; &quot;);
                else if(m_content[j-m_padding_left])
                    printf(&quot;%c&quot;, m_content[i-m_padding_top][j-m_padding_left]);
                else break;
            }
        }
        if(i+1 != m_height) printf(&quot;\n&quot;);
    }
}</code></pre>
<h3><a href="#guan-li-nei-cun" class="header-anchor"># </a><span id="guan-li-nei-cun">管理内存</span></h3><p>对于 Picture 来说，管理内存比较容易，只需要给其添加一个析构函数即可。</p>
<pre><code class="cpp">// picture.h
@@ -5,6 +5,7 @@
 {
 public:
        Picture(const char **_Strs, int _Lines);
+       ~Picture(void);
        void print(void);
 private:
        char **m_content;</code></pre>
<pre><code class="cpp">@@ -23,6 +23,13 @@
        }
 }

+Picture::~Picture(void)
+{
+       for(int i=0; i&lt;m_height; ++i)
+               free(m_content[i]);
+       free(m_content);
+}
+
 void Picture::print(void)
 {
        for(int i=0; i&lt;m_height; ++i)</code></pre>
<h3><a href="#zen-me-zong-xiang-lian-jie-fu-kan-ji-cheng-guan-xi" class="header-anchor"># </a><span id="zen-me-zong-xiang-lian-jie-fu-kan-ji-cheng-guan-xi">怎么纵向连接？俯瞰继承关系</span></h3><p>首先我们要知道，纵向连接后的结果也应该是一个 Picture，并能够继续纵向连接。</p>
<h4><a href="#wo-men-ying-gai-zen-yang-she-ji-ni" class="header-anchor"># </a><span id="wo-men-ying-gai-zen-yang-she-ji-ni">我们应该怎样设计呢？</span></h4><p>这里引入一个概念：继承</p>
<p>如下，是图形之间的继承关系。<br>我们可以认为，如果 A 指向 B ，那么 B 属于 A。</p>
<p><em>这里涉及到面向对象设计的基本理念，目前主流思路有两种：is-a，has-a <del>和 like-a</del>。</em></p>
<pre class="mermaid">graph TB;
    Shape-->Polygen;
    Shape-->Ellipse;
    Polygen-->Rectange;
    Ellipse-->Circle;
    Rectange-->Square;</pre>

<p>在我们这个例子中，我们有理由认为，Picture 是所有类型的基础。<br>我们可以想当然如此构造：</p>
<pre class="mermaid">graph TB;
    Picture --> vCatPicture;
    Picture --> hCatPicture;</pre>

<p>这时候出现了一个问题：我们连接两个图像的时候，应该使用类型本身呢，还是使用其指针呢？<br>例如上面的图形例子，Rectangle 类型（通常）可以无损转化为 Square 类型，但是 Rectangle 类型的指针不能转换为 Square 类型的指针。相反的，Square 类型（通常）不能无损转换为 Rectangle 类型，但是 Square * 类型却可以实现到 Rectangle * 的转换。</p>
<p>指针类型并不方便，而值类型（及引用类型）并不能支持这样的转换。</p>
<p>如果采用指针类型的话，我们需要定制这样的框架：</p>
<pre class="mermaid">graph TB;
    Picture-->BasicPicture;
    Picture-->vCatPicture;
    Picture-->hCatPicture;
    Picture-->FramedPicture;</pre>

<p>其中，<code>BasicPicture</code> 表示纯文本的图，<code>vCatPicture</code> 表示经过了一次垂直连接，<code>hCatPicture</code> 表示经过一次水平连接，<code>FramedPicture</code> 表示加框架的图。<br>在具体使用时，我们使用 Picture * 来表示所有可能的类型。</p>
<p>而值类型则不同，值类型只能从上游向下游转换（从亲代到子代），因此只能是这两种情况之一（或其组合形式）。</p>
<pre class="mermaid">graph TB;
    A-->D;
    B-->D;
    C-->D;
    a==>b;
    b==>c;
    c==>d;</pre>

<p>显然，这两种构造都无法用 is-a 来解释，用 has-a 解释也很勉强。因此，这是一种不合理的构造方式。</p>
<p><strong>因此，我们采用指针来进行运算，采用共同基类来实现</strong>。</p>
<hr>
<h4><a href="#wo-men-xu-yao-na-xie-lei-xing-ni" class="header-anchor"># </a><span id="wo-men-xu-yao-na-xie-lei-xing-ni">我们需要哪些类型呢？</span></h4><p><code>BasicPicture</code> ：保存实际的图像（字符串）内容，及其宽度与高度。<br><code>vCatPicture</code> ，<code>hCatPicture</code> ：保存其所连接的两图像指针<br><code>FramedPicture</code> ：保存一个图像指针及其边框内容。</p>
<p>基于这些类型，我们可以容易地实现图像连接，图像解除连接，图像加边框，图像去边框以及修改内容。</p>
<hr>
<h4><a href="#wo-men-zen-yang-guan-li-nei-cun" class="header-anchor"># </a><span id="wo-men-zen-yang-guan-li-nei-cun">我们怎样管理内存？</span></h4><p>之前的例子里，我们采用构造函数分配内存，析构函数释放内存的方式来处理内存问题。</p>
<p>但是，如下所示的，在断点处，<code>FramedPicture b</code> 的内容物（a）已经被析构，因此输出 b 可能是一个非法操作。</p>
<pre><code class="cpp">FramedPicture b;
{
    BasicPicture a = ...;
    b = enframe(a);
}
// breakpoint</code></pre>
<p>经过一系列思考，我们可以推理出引用计数的必要性。</p>
<h3><a href="#lei-xing-de-she-ji" class="header-anchor"># </a><span id="lei-xing-de-she-ji">类型的设计</span></h3><h4><a href="#picture-lei" class="header-anchor"># </a><span id="picture-lei">Picture 类</span></h4><p>经过思考，我们设计这样一个 <code>Picture</code> 类。</p>
<pre><code class="cpp">class Picture
{
public:
    Picture(void);
    Picture(const char* const *, int);
    Picture(const Picture&amp;);
    ~Picture(void);

    Picture&amp; operator=(const Picture&amp;);
    void reframe(int p, char c);
    void reframe(char c, char v, char h);
    Picture split(int id);
private:
    PictureEntity *ap ;
    Picture(PictureEntity* p);

    int width(void) const;
    int height(void) const;
    std::ostream&amp; display(std::ostream&amp;, int, int) const;
};</code></pre>
<p>最上面的三个构造函数，分别有以下作用：</p>
<pre><code class="cpp">Picture::Picture(void); // 构造一个空的 Picture 对象
Picture::Picture(const char* const *, int); // 根据字符串指针和其行数构造一个 Picture
Picture::Picture(const Picture&amp;); // 拷贝构造函数，例如</code></pre>
<p><code>reframe</code> 函数用来重设框架格式。<br><code>private</code> 内容则是对外不可见的具体实现。<br><code>PictureEntity *ap</code> 是一个指针，这里的 <code>PictureEntity</code> 即为上面设计中的所有类型的基类 。<br>剩余的函数姑且不考虑。</p>
<h4><a href="#pictureentity-ji-lei" class="header-anchor"># </a><span id="pictureentity-ji-lei">PictureEntity 基类</span></h4><p>这个类型，作为所有行为的具体呈现者，并且是一个基类，我们如此定义：</p>
<pre><code class="cpp">class PictureEntity
{
public:
    PictureEntity(void);
    virtual ~PictureEntity(void) = 0;
    virtual void incuse(void) = 0;
    virtual int decuse(void) = 0;
    friend class Picture;
protected:
    int use;
    virtual int width(void) const = 0;
    virtual int height(void) const = 0;
    virtual ostream&amp; display(ostream&amp;, int, int) const = 0;
    ostream&amp; fillEmpty(ostream&amp;, int, int, char _ch = &#39; &#39;) const;
    static int max(int a, int b);
    virtual void reframe(int pos, char c);
    virtual void reframe(char corner, char vlimit, char hlimit);
    virtual Picture split(int id) = 0;
}</code></pre>
<p>首先看 <code>public:</code> 部分：<br>继承中的基类必须拥有虚析构函数，在这里，为了防止创建 <code>PictureEntity</code> 的实例，我们将其析构函数设置为纯虚析构函数。<br>其次，其拥有两个对引用进行处理的函数，且均为纯虚函数。</p>
<p>其次看 <code>protected</code> ：<br><code>use</code> 为引用计数器。<br>其他的函数作用如其名所示。</p>
<p>这个类无法被实例化。</p>
<h4><a href="#basicpicture" class="header-anchor"># </a><span id="basicpicture">BasicPicture</span></h4><pre><code class="cpp">class BasicPicture : public PictureEntity
{
public:
    BasicPicture(const char* const*, int);
    ~BasicPicture(void);
    void incuse(void);
    int decuse(void);

    int width(void) const;
    int height(void) const;
    ostream&amp; display(ostream&amp;, int, int)const;

    Picture split(int id);
private:
    char **m_data;
    int m_size;
    int *m_width;
    int m_maxwidth;
};</code></pre>
<p>这个类型看起来更像是我们在<a href="#构造和输出功能">前面</a> 里面实现的类型。其用法也与之相似。</p>
<h4><a href="#framedpicture" class="header-anchor"># </a><span id="framedpicture">FramedPicture</span></h4><pre><code class="cpp">class FramedPicture : public PictureEntity
{
public:
    FramedPicture(const Picture&amp;, char c, char v, char h);
    FramedPicture(const Picture&amp;, char ct, char cb, char v, char h);
    FramedPicture(const Picture&amp;, char ct, char cb, char t, char b, char l, char r);
    FramedPicture(const Picture&amp;, char c1, char c2, char c3, char c4, char t, char b, char l, char r);
    ~FramedPicture(void);
    void incuse(void);
    int decuse(void);

    void reframe(int pos, char c);
    void reframe(char corner, char v, char h);

    int width(void) const;
    int height(void) const;
    ostream&amp; display(ostream&amp;, int y, int mw) const;

    Picture split(int id);

    friend Picture enframe(const Picture&amp;);
private:
    Picture m_insidePic;
    char c1, c2, c3, c4;
    char l, r, t, b;
};</code></pre>
<p>这个类型 <code>public:</code> 部分有大量构造函数，其余部分与上面的<code>BasicPicture</code> 相似。</p>
<h4><a href="#vcatpicture" class="header-anchor"># </a><span id="vcatpicture">VcatPicture</span></h4><pre><code class="cpp">class VcatPicture : public PictureEntity
{
public:
    VcatPicture(const Picture&amp;, const Picture&amp;);
    ~VcatPicture(void);
    void incuse(void);
    int decuse(void);

    int width(void)const ;
    int height(void)const ;
    ostream&amp; display(ostream&amp;, int y, int mw) const;
    Picture split(int id);

    friend Picture operator&amp;(const Picture&amp;, const Picture&amp;);
private:
    Picture top, bottom;
};</code></pre>
<p>这个类表示 <code>top</code> 和 <code>bottom</code> 纵向连接。</p>
<h4><a href="#hcatpicture" class="header-anchor"># </a><span id="hcatpicture">HcatPicture</span></h4><pre><code class="cpp">class HcatPicture :public PictureEntity
{
public:
    HcatPicture(const Picture&amp;, const Picture&amp;);
    ~HcatPicture(void);
    void incuse(void);
    int decuse(void);

    int width(void)const ;
    int height(void)const ;
    ostream&amp; display(ostream&amp;, int y, int mw) const;
    Picture split(int id);

    friend Picture operator|(const Picture&amp;, const Picture&amp;);
private:
    Picture left, right;
};</code></pre>
<hr>
<h2><a href="#zong-shu-ji-xiao-guo-zhan-shi" class="header-anchor"># </a><span id="zong-shu-ji-xiao-guo-zhan-shi">总述及效果展示</span></h2><p>通过上面从<a href="#怎么纵向连接？俯瞰继承关系">继承关系设计</a> 到 <a href="#类型的设计">类型的具体设计</a> ，我们基本有了程序的框架。</p>
<p>在经过具体实现这些类型后，我们就得到了完整的<a href="../../../../resources/CharacterPicture.zip">代码</a>。</p>
<h3><a href="#cpp-ke-hou-xi-ti" class="header-anchor"># </a><span id="cpp-ke-hou-xi-ti">Cpp 课后习题</span></h3><ul>
<li>课后习题 1 : 经过上面的示例，我们可以发现一个给定的图可以唯一确定一个（系列性的）输出动作，而 <code>std::string</code> 也可以唯一确定一个输出动作，请将一个给定的图转换为 <code>std::string</code> 。</li>
<li>课后习题 2 : 请设计一个类，可以独立保存一张图及其结构信息。</li>
<li>课后习题 3 : 习题 2 自然不难，能否将这个类的 <code>std::cin</code> 和<code>std::cout</code> 的对应操作重载，使其能够被保存和读取。</li>
<li>课后习题 4 : 如果将<code>&#39;*&#39;</code> 视为黑色，<code>&#39; &#39;</code> 视为白色，那么任何一张只有黑白两色的图可以唯一确定一个字符图，请设计一个方法，能够根据一张图片（<code>.jpg</code> 或<code>.png</code>）生成一个字符图。（可以利用第三方库读入图片）</li>
<li>课后习题 5 : 相信对于聪明的读者们，习题 4 比较容易。请在习题 4 的基础上，支持字符图的旋转、平移等图形操作。</li>
<li>课后习题 6 : 其实根据字符密度，<a href="../../../../resources/thirdeye.txt">纯字符方式</a>可以表现黑白灰色彩（单通道八位），请在习题 4 的基础上，生成一个可以生成黑白灰色彩的图。（如 <code>&#39; &#39;</code> 表示纯黑，<code>&#39;■&#39;</code> 表示纯白，<code>&#39;*&#39;</code> 表示一种灰色）</li>
</ul>
]]></content>
      
        
        <tags>
            
            <tag> 社团课程 </tag>
            
        </tags>
        
    </entry>
    
    <entry>
      <title><![CDATA[matplotlib编码字体错误的解决过程]]></title>
      <url>/2019/11/07/matplotlib%E7%BC%96%E7%A0%81%E5%AD%97%E4%BD%93%E9%94%99%E8%AF%AF%E7%9A%84%E8%A7%A3%E5%86%B3%E8%BF%87%E7%A8%8B/</url>
      <content type="html"><![CDATA[<h2><a href="#fa-xian-wen-ti" class="header-anchor"># </a><span id="fa-xian-wen-ti">发现问题</span></h2><p>众所周知，设置 matplotlib 输出中文需要：</p>
<p>plt.rcParams[‘font.sans-serif’]=[‘SimHei’]  # 用来正常显示中文标签<br>plt.rcParams[‘axes.unicode_minus’]=False  # 用来正常显示负号</p>
<a id="more"></a>

<p>第一行是设置字体（推荐使用 <code>.insert(0, fontname)</code> 而非直接赋值）</p>
<p>第二行则是设置减号（<code>-</code> ）采用 ASCII 码查找字体，而非 unicode 编码。</p>
<p>但是，在使用过程中发生了错误。</p>
<p>复现错误：</p>
<pre><code class="python"># -*- coding: UTF-8 -*-
%matplotlib inline

import matplotlib.pyplot as plt

plt.rcParams[&#39;font.sans-serif&#39;]=[&#39;SimHei&#39;]
plt.rcParams[&#39;axes.unicode_minus&#39;]=False



plt.figure(figsize=(18,9))

data = [0.001*(10**i) for i in range(4)]
bins = 10**(np.linspace(-3, 2))
plt.hist(data, bins = bins, label = &#39;test&#39;)

plt.xscale(&#39;log&#39;)
plt.ylabel(&#39;English test&#39;)
plt.title(&#39;汉字测试&#39;)

plt.show()</code></pre>
<p>会发生这样的错误：</p>
<blockquote>
<pre><code>%pythonlib%\matplotlib\mathtext.py:849: MathTextWarning: Font &#39;default&#39; does not have a glyph for &#39;-&#39; [U+2212]
  MathTextWarning)
%pythonlib%\matplotlib\mathtext.py:850: MathTextWarning: Substituting with a dummy symbol.
  warn(&quot;Substituting with a dummy symbol.&quot;, MathTextWarning)</code></pre></blockquote>
<p>我们可以认为是 ‘-‘ 导致的出错。</p>
<p>通过在 github 上的 <a href="https://github.com/matplotlib/matplotlib" target="_blank" rel="noopener">matplotlib 项目</a> 查找，可以找到这个文件 <a href="https://github.com/matplotlib/matplotlib/blob/master/examples/text_labels_and_annotations/unicode_minus.py" target="_blank" rel="noopener">matplotlib/examples/text_labels_and_annotations/unicode_minus.py</a> ，通过看文件名，大致可知其为 <code>unicode_minus</code> 的示例文件。</p>
<p>内容如下：</p>
<pre><code class="python">&quot;&quot;&quot;
=============
Unicode minus
=============
By default, tick labels at negative values are rendered using a `Unicode
minus`__ (U+2212) rather than an ASCII hyphen (U+002D).  This can be controlled
by setting :rc:`axes.unicode_minus`.
__ https://en.wikipedia.org/wiki/Plus_and_minus_signs#Character_codes
The replacement is performed at draw time of the tick labels (usually during a
`.pyplot.show()` or `.pyplot.savefig()` call). Therefore, all tick labels of
the figure follow the same setting and we cannot demonstrate both glyphs on
real tick labels of the same figure simultaneously.
Instead, this example simply showcases the difference between the two glyphs
in a magnified font.
&quot;&quot;&quot;

import matplotlib.pyplot as plt

fig = plt.figure(figsize=(4, 2))
fig.text(.15, .6, &quot;Unicode minus:&quot;, fontsize=20)
fig.text(.85, .6, &quot;\N{MINUS SIGN}1&quot;, ha=&#39;right&#39;, fontsize=20)
fig.text(.15, .3, &quot;ASCII hyphen:&quot;, fontsize=20)
fig.text(.85, .3, &quot;-1&quot;, ha=&#39;right&#39;, fontsize=20)
plt.show()</code></pre>
<p>可知，<code>unicode_minus</code> 设置为 <code>False</code> 时，理应不使用 U+2212 输出，而是使用 U+002D，然而上面的错误信息却试图输出 U+2212，我们可以认为是这个选项没有起到作用。</p>
<h2><a href="#ding-wei-wen-ti" class="header-anchor"># </a><span id="ding-wei-wen-ti">定位问题</span></h2><p>通过注释，我们可以发现出现警告的原因是<code>plt.xscale(&#39;log&#39;)</code> 一行。</p>
<p>不妨在库源码中追溯来源。</p>
<pre><code class="python">import matplotlib.pyplot as plt</code></pre>
<p>由此可知，错误是在 <code>matplotlib.pyplot.xscale</code> 调用时出现的。</p>
<p>追溯<code>matplotlib.pyplot.xscale</code> 可得：</p>
<pre><code class="python">def xscale(*args, **kwargs):
    gca().set_xscale(*args, **kwargs)</code></pre>
<p>首先判断 <code>gca()</code> 类型，由 <code>print(gca().__module__)</code> 输出 <code>matplotlib.axes._subplots</code> 。</p>
<p>打开对应模块，会发现找不到 <code>gca()</code> 的定义。</p>
<p>改为 <code>print(type(gca()))</code> ，输出<code>&lt;class &#39;matplotlib.axes._subplots.AxesSubplot&#39;&gt;</code> 。</p>
<p>虽然未能找到 <code>AxesSubplot</code> ，但是观察可得，<code>AxesSubplot</code> 应该是 <code>matplotlib.axes._subplots.SubplotBase</code>  的一个子类/别名。</p>
<p>然而，在该基类中未能找到对应的函数。</p>
<p>由于 python 具有反射机制，我们想当然地去<code>matplotlib\axes\_base.py</code> 中，在<code>matplotlib.axes._base._AxesBase</code> 内，可以找到</p>
<pre><code class="python">    def set_xscale(self, value, **kwargs):
        g = self.get_shared_x_axes()
        for ax in g.get_siblings(self):
            ax.xaxis._set_scale(value, **kwargs)
            ax._update_transScale()
            ax.stale = True

        self.autoscale_view(scaley=False)</code></pre>
<p>简单分析可知，我们出错的情况下， <code>set_xscale</code> 传入参数为 <code>set_xscale(this, &#39;log&#39;)</code> 。</p>
<p>也即通过该函数呈递给  <code>ax.xaxis._set_scale(value, **kwargs)</code> ，此时<code>value = &#39;log&#39;, kwargs={}</code> 。</p>
<p>在呈递前插入</p>
<pre><code class="python">            print(type(ax.xaxis))</code></pre>
<p>得到输出<code>&lt;class &#39;matplotlib.axis.XAxis&#39;&gt;</code> 。</p>
<p>由于<code>XAxis</code> 继承于<code>Axis</code> ，搜索<code>_set_scale</code> 可发现其定义于后者，即基类中。</p>
<pre><code class="python">    def _set_scale(self, value, **kwargs):
        self._scale = mscale.scale_factory(value, self, **kwargs)
        self._scale.set_default_locators_and_formatters(self)

        self.isDefault_majloc = True
        self.isDefault_minloc = True
        self.isDefault_majfmt = True
        self.isDefault_minfmt = True</code></pre>
<p>通过尝试输出<code>self._scale</code> ，可以得到缩放类型为<code>&quot;log&quot;</code> 时，该变量类型为<code>matplotlib.scale.LogScale</code> 。</p>
<p>目前，我们可以得到这样的呈递关系图：</p>
<pre class="mermaid">graph TB;
    plt-->gca;
    gca-->axis;
    axis-->mscale;

plt[pyplot.xscale&#40'log'&#41]
gca[axes._subplots.set_xscale&#40'log'&#41]
axis[axis.XAxis._set_scale&#40'log'&#41]
mscale[scale.LogScale.set_default_locators_and_formatters&#40&#41]</pre>

<p>根据</p>
<pre><code class="python">class LogitScale(ScaleBase):
    ...</code></pre>
<pre><code class="python">_scale_mapping = {
    &#39;linear&#39;: LinearScale,
    &#39;log&#39;:    LogScale,
    &#39;symlog&#39;: SymmetricalLogScale,
    &#39;logit&#39;:  LogitScale,
    }</code></pre>
<pre><code class="python">def scale_factory(scale, axis, **kwargs):
    &quot;&quot;&quot;
    Return a scale class by name.

    ACCEPTS: [ %(names)s ]
    &quot;&quot;&quot;
    scale = scale.lower()
    if scale is None:
        scale = &#39;linear&#39;

    if scale not in _scale_mapping:
        raise ValueError(&quot;Unknown scale type &#39;%s&#39;&quot; % scale)

    return _scale_mapping[scale](axis, **kwargs)
scale_factory.__doc__ = cbook.dedent(scale_factory.__doc__) % \
    {&#39;names&#39;: &quot; | &quot;.join(get_scale_names())}</code></pre>
<p>可得<code>self._scale = mscale.scale_factory(&#39;log&#39;)</code> 等价于<code>self._scale = mscale.LogScale(self)</code> 。</p>
<p>到 <code>class &#39;matplotlib.scale.LogScale&#39;</code> 中可以看到方法</p>
<pre><code class="python">    def set_default_locators_and_formatters(self, axis):
        &quot;&quot;&quot;
        Set the locators and formatters to specialized versions for
        log scaling.
        &quot;&quot;&quot;
        axis.set_major_locator(LogLocator(self.base))
        axis.set_major_formatter(LogFormatterSciNotation(self.base))
        axis.set_minor_locator(LogLocator(self.base, self.subs))
        axis.set_minor_formatter(
            LogFormatterSciNotation(self.base,
                                    labelOnlyBase=(self.subs is not None)))</code></pre>
<p>观察易知，其分别设置刻度的标值和格式。</p>
<p>将设置四行改为</p>
<pre><code class="python">        axis.set_major_locator(NullLocator())
        axis.set_major_formatter(NullFormatter())
        axis.set_minor_locator(NullLocator())
        axis.set_minor_formatter(NullFormatter())</code></pre>
<p>可以发现，<code>plt.show()</code> 没有了水平刻度值，也没有警告信息。</p>
<p>通过观察可知，<code>set_major_XXX</code> 控制刻度值，而<code>set_minor_XXX</code> 控制刻度格。</p>
<p>并且错误原因在于<code>axis.set_major_formatter(LogFormatterSciNotation(self.base))</code> 。</p>
<p>因为 <code>LogScale</code> 默认以 10 为底（<code>self.base</code> ），因此可知该句等价于</p>
<p><code>Axis.set_major_formatter(LogFormatterSciNotation(10))</code></p>
<p>其中，<code>LogFormatterSciNotation</code> 类型为 <code>matplotlib.ticker.LogFormatterSciNotation</code> 。</p>
<hr>
<p>根据输出的警告信息，我们在对应位置前生成一个异常，可以得到</p>
<pre><code class="python">matplotlib\mathtext.py in __init__(self, c, state, math)
   1454         # The real width, height and depth will be set during the
   1455         # pack phase, after we know the real fontsize
-&gt; 1456         self._update_metrics()
   1457 
   1458     def __internal_repr__(self):

matplotlib\mathtext.py in _update_metrics(self)
   1461     def _update_metrics(self):
   1462         metrics = self._metrics = self.font_output.get_metrics(
-&gt; 1463             self.font, self.font_class, self.c, self.fontsize, self.dpi, self.math)
   1464         if self.c == &#39; &#39;:
   1465             self.width = metrics.advance

matplotlib\mathtext.py in get_metrics(self, font, font_class, sym, fontsize, dpi, math)
    456             &quot;height&quot;.
    457         &quot;&quot;&quot;&quot;&quot;&quot;&quot; # 为了不影响代码高亮，补全引号
--&gt; 458         info = self._get_info(font, font_class, sym, fontsize, dpi, math)
    459         return info.metrics
    460 

matplotlib\mathtext.py in _get_info(self, fontname, font_class, sym, fontsize, dpi, math)
    579 
    580         font, num, symbol_name, fontsize, slanted = \
--&gt; 581             self._get_glyph(fontname, font_class, sym, fontsize, math)
    582 
    583         font.set_size(fontsize, dpi)

matplotlib\mathtext.py in _get_glyph(self, fontname, font_class, sym, fontsize, math)
    907             # otherwise return regular glyph
    908             return super(DejaVuFonts, self).\_get_glyph(fontname,
--&gt; 909                     font_class, sym, fontsize, math)
    910 
    911 

matplotlib\mathtext.py in _get_glyph(self, fontname, font_class, sym, fontsize, math)
    787 
    788     def _get_glyph(self, fontname, font_class, sym, fontsize, math=True):
--&gt; 789         raise &quot;test&quot;
    790         found_symbol = False
    791 </code></pre>
<p>此时的 fontname 为 ‘default’，应当改为其他内容（以满足条件 <code>fontname in (&#39;it&#39;, &#39;regular&#39;)</code> ）</p>
<hr>
<p>经测试，改 UnicodeFonts 类中 _get_glyph 内 fontname 为 ‘it’ 可解决问题。</p>
<p>经测试，改 TruetypeFonts 类中 _get_info 方法的 fontname 为 ‘it’ 也可解决问题。</p>
<p>经测试，改 Fonts 基类中的 get_metrics 方法的 font 为 ‘it’ 无效。</p>
<p>在 _get_info 添加输出，可以发现其被多处调用。</p>
<p>重新获取另一调用链，可得</p>
<pre><code class="python">matplotlib\mathtext.py in group(self, s, loc, toks)
   2814 
   2815     def group(self, s, loc, toks):
-&gt; 2816         grp = Hlist(toks[0])
   2817         return [grp]
   2818     required_group = simple_group = group

matplotlib\mathtext.py in __init__(self, elements, w, m, do_kern)
   1609         List.__init__(self, elements)
   1610         if do_kern:
-&gt; 1611             self.kern()
   1612         self.hpack()
   1613 

matplotlib\mathtext.py in kern(self)
   1631 
   1632                 new_children.append(elem)
-&gt; 1633                 kerning_distance = elem.get_kerning(next)
   1634                 if kerning_distance != 0.:
   1635                     kern = Kern(kerning_distance)

matplotlib\mathtext.py in get_kerning(self, next)
   1486                 self.font, self.font_class, self.c, self.fontsize,
   1487                 next.font, next.font_class, next.c, next.fontsize,
-&gt; 1488                 self.dpi)
   1489         return advance + kern
   1490 

matplotlib\mathtext.py in get_kern(self, font1, fontclass1, sym1, fontsize1, font2, fontclass2, sym2, fontsize2, dpi)
    634                  font2, fontclass2, sym2, fontsize2, dpi):
    635         if font1 == font2 and fontsize1 == fontsize2:
--&gt; 636             info1 = self._get_info(font1, fontclass1, sym1, fontsize1, dpi)
    637             info2 = self._get_info(font2, fontclass2, sym2, fontsize2, dpi)
    638             font = info1.font

matplotlib\mathtext.py in _get_info(self, fontname, font_class, sym, fontsize, dpi, math)
    580 
    581         font, num, symbol_name, fontsize, slanted = \
--&gt; 582             self._get_glyph(fontname, font_class, sym, fontsize, math)
    583 
    584         font.set_size(fontsize, dpi)

matplotlib\mathtext.py in _get_glyph(self, fontname, font_class, sym, fontsize, math)
    909             # otherwise return regular glyph
    910             return super(DejaVuFonts, self)._get_glyph(fontname,
--&gt; 911                     font_class, sym, fontsize, math)
    912 
    913 

matplotlib\mathtext.py in _get_glyph(self, fontname, font_class, sym, fontsize, math)
    840                 else:
    841                     return self.cm_fallback._get_glyph(
--&gt; 842                         fontname, font_class, sym, fontsize)
    843             else:
    844                 if fontname in (&#39;it&#39;, &#39;regular&#39;) and isinstance(self, StixFonts):

matplotlib\mathtext.py in _get_glyph(self, fontname, font_class, sym, fontsize, math)
    845                     return self._get_glyph(&#39;rm&#39;, font_class, sym, fontsize)
    846                 print(fontname)
--&gt; 847                 raise &#39;error&#39;
    848                 warn(&quot;Font &#39;%s&#39; does not have a glyph for &#39;%s&#39; [U+%x]&quot; %
    849                      (new_fontname,</code></pre>
<p>为了方便此后调试，在改动部分加上 <code># RecoDebugging</code> 。</p>
<p>该次 Bug 出错在于 <code>mathtext.Char.font = &#39;default&#39;</code> ，经过捕捉可发现其被 <code>state.font</code> 赋值，即 <code>matplotlib.mathtext.Parser.State.font</code> 。</p>
<p>追踪这个构造函数调用，可得</p>
<pre><code class="python">matplotlib\mathtext.py in symbol(self, s, loc, toks)
   2638         c = toks[0]
   2639         try:
-&gt; 2640             char = Char(c, self.get_state())
   2641         except ValueError:
   2642             raise ParseFatalException(s, loc, &quot;Unknown symbol: %s&quot; % c)

matplotlib\mathtext.py in __init__(self, c, state, math)
   1449         self.c = c
   1450         self.font_output = state.font_output
-&gt; 1451         raise &#39;test&#39;
   1452         self.font = state.font
   1453         self.font_class = state.font_class</code></pre>
<p>观察 <code>matplotlib.mathtext.Parser.symbol</code> 可得，<code>Parser.State</code> 构造函数被调用于 </p>
<pre><code class="python">matplotlib\mathtext.py in parse(self, s, fonts_object, fontsize, dpi)
   2513         Returns the parse tree of :class:`Node` instances.
   2514         &quot;&quot;&quot;&quot;&quot;&quot; # end the comment
-&gt; 2515         self._state_stack = [self.State(fonts_object, &#39;default&#39;, &#39;rm&#39;, fontsize, dpi)]
   2516         self._em_width_cache = {}
   2517         try:

C:\ProgramData\Anaconda3\lib\site-packages\matplotlib\mathtext.py in __init__(self, font_output, font, font_class, fontsize, dpi)
   2540         &quot;&quot;&quot;&quot;&quot;&quot;  # end the comment
   2541         def __init__(self, font_output, font, font_class, fontsize, dpi):
-&gt; 2542             raise &#39;generate state&#39;
   2543             self.font_output = font_output
   2544             self._font = font</code></pre>
<p>最终可以发现，<code>state</code> 被赋值于此处，只需要修改 <code>matplotlib.mathtext.Parser.parse</code> 即可结束此支错误。</p>
<hr>
<p>暂改为 ‘it’，运行仍然出错，主动抛出异常，捕获链如下：</p>
<pre><code class="python">matplotlib\mathtext.py in group(self, s, loc, toks)
   2815 
   2816     def group(self, s, loc, toks):
-&gt; 2817         grp = Hlist(toks[0])
   2818         return [grp]
   2819     required_group = simple_group = group

matplotlib\mathtext.py in __init__(self, elements, w, m, do_kern)
   1609         List.__init__(self, elements)
   1610         if do_kern:
-&gt; 1611             self.kern()
   1612         self.hpack()
   1613 

matplotlib\mathtext.py in kern(self)
   1631 
   1632                 new_children.append(elem)
-&gt; 1633                 kerning_distance = elem.get_kerning(next)
   1634                 if kerning_distance != 0.:
   1635                     kern = Kern(kerning_distance)

matplotlib\mathtext.py in get_kerning(self, next)
   1486                 self.font, self.font_class, self.c, self.fontsize,
   1487                 next.font, next.font_class, next.c, next.fontsize,
-&gt; 1488                 self.dpi)
   1489         return advance + kern
   1490 

matplotlib\mathtext.py in get_kern(self, font1, fontclass1, sym1, fontsize1, font2, fontclass2, sym2, fontsize2, dpi)
    634                  font2, fontclass2, sym2, fontsize2, dpi):
    635         if font1 == font2 and fontsize1 == fontsize2:
--&gt; 636             info1 = self._get_info(font1, fontclass1, sym1, fontsize1, dpi)
    637             info2 = self._get_info(font2, fontclass2, sym2, fontsize2, dpi)
    638             font = info1.font

matplotlib\mathtext.py in _get_info(self, fontname, font_class, sym, fontsize, dpi, math)
    580 
    581         font, num, symbol_name, fontsize, slanted = \
--&gt; 582             self._get_glyph(fontname, font_class, sym, fontsize, math)
    583 
    584         font.set_size(fontsize, dpi)

matplotlib\mathtext.py in _get_glyph(self, fontname, font_class, sym, fontsize, math)
    909             # otherwise return regular glyph
    910             return super(DejaVuFonts, self)._get_glyph(fontname,
--&gt; 911                     font_class, sym, fontsize, math)
    912 
    913 

matplotlib\mathtext.py in _get_glyph(self, fontname, font_class, sym, fontsize, math)
    840                 else:
    841                     return self.cm_fallback._get_glyph(
--&gt; 842                         fontname, font_class, sym, fontsize)
    843             else:
    844                 if fontname in (&#39;it&#39;, &#39;regular&#39;) and isinstance(self, StixFonts):

matplotlib\mathtext.py in _get_glyph(self, fontname, font_class, sym, fontsize, math)
    845                     return self._get_glyph(&#39;rm&#39;, font_class, sym, fontsize)
    846                 #print(fontname)
--&gt; 847                 raise &#39;error&#39; # RecoDebugging
    848                 warn(&quot;Font &#39;%s&#39; does not have a glyph for &#39;%s&#39; [U+%x]&quot; %
    849                      (new_fontname,</code></pre>
]]></content>
      
        
        <tags>
            
            <tag> python matplotlib </tag>
            
        </tags>
        
    </entry>
    
    <entry>
      <title><![CDATA[制作主题时踩的坑]]></title>
      <url>/2019/10/22/%E5%88%B6%E4%BD%9C%E4%B8%BB%E9%A2%98%E6%97%B6%E8%B8%A9%E7%9A%84%E5%9D%91/</url>
      <content type="html"><![CDATA[<p>自定义主题的时候遇到的一些问题记录如下：</p>
<a id="more"></a>

<ol>
<li>设置好 index.ejs 布局后 hexo generate 出错</li>
</ol>
<pre><code class="bash">ERROR D:\GitHub\RedContritio.github.io\themes\landscape\layout\index.ejs:1
 &gt;&gt; 1| &lt;% page.posts.each(function(post){ %&gt;
    2|
    3| &lt;% }) %&gt;

Cannot read property &#39;each&#39; of undefined</code></pre>
<p>这个现象会出现是因为没有找到其他的布局框架，导致 post 也用 index.ejs 布局，但是 post 页面内无法进行 posts 的迭代从而出错。</p>
]]></content>
      
        
        <tags>
            
            <tag> 维护 </tag>
            
        </tags>
        
    </entry>
    
    <entry>
      <title><![CDATA[Cpp 入门]]></title>
      <url>/2019/10/20/basic-cpp-knowledge/</url>
      <content type="html"><![CDATA[<blockquote><p>C++ is a horrible language. It’s made more horrible by the fact that a lot of substandard programmers use it, to the point where it’s much much easier to generate total and utter crap with it. </p>
<footer><strong>[Linus Torvalds]</strong></footer></blockquote>

<a id="more"></a>

<p>为了便于讲解，本文采用由浅及深的方式组织行文主线。</p>
<p>本文主要采用对比 c 和 cpp 差异的方式来进行。</p>
<hr>
<h1><a href="#tou-wen-jian" class="header-anchor"># </a><span id="tou-wen-jian">头文件</span></h1><p>几乎所有的 c 语言代码都需要头文件，cpp 和 c 的头文件对比是开始编程前必须的一步。</p>
<p><em>几乎所有的 c 代码中 <code>#include</code> 命令都可以直接复制到 cpp 中使用，但是这并不推荐</em></p>
<h3><a href="#include-shi-shi-me" class="header-anchor"># </a><span id="include-shi-shi-me">#include 是什么？</span></h3><pre><code class="c">// module.txt
printf(&quot;%d\n&quot;, a);
a *= 3;</code></pre>
<pre><code class="c">// main.c
#include &lt;stdio.h&gt;

int main()
{
    int a = 1, b = 2;
#include &quot;module.txt&quot;
    printf(&quot;%d %d\n&quot;, a, b);
    return 0;
}</code></pre>
<p>编译执行 main.c 后，输出应该是</p>
<blockquote>
<p>1</p>
<p>3 2</p>
</blockquote>
<p>可见，<code>#include</code> 作用是将一段文本直接引入到代码中。</p>
<p>对头文件使用 <code>#include</code> 时也是如此。</p>
<h3><a href="#tou-wen-jian-ming-cheng-dui-bi" class="header-anchor"># </a><span id="tou-wen-jian-ming-cheng-dui-bi">头文件名称对比</span></h3><table>
<thead>
<tr>
<th>语言</th>
<th>———–</th>
<th>————</th>
<th>———–</th>
<th>———–</th>
<th>———–</th>
<th>———–</th>
</tr>
</thead>
<tbody><tr>
<td>c</td>
<td>stdio.h</td>
<td>stdlib.h</td>
<td>math.h</td>
<td>\</td>
<td>windows.h</td>
<td>string.h</td>
</tr>
<tr>
<td>cpp</td>
<td>cstdio</td>
<td>cstdlib</td>
<td>cmath</td>
<td>vector</td>
<td>windows.h</td>
<td>cstring</td>
</tr>
</tbody></table>
<p>可见，对于常见的头文件，是将 c 头文件的 .h 去掉，在前面加上 c，如 <font size="5"><font color="#green">c</font>stdio<font color="red">.h</font> </font> 。</p>
<p>尤其注意 string.h 文件，cstring 和 string 完全不同。</p>
<pre><code class="c">// cstdio
...
#include &lt;stdio.h&gt;
...</code></pre>
<hr>
<h1><a href="#iostream" class="header-anchor"># </a><span id="iostream">IOSTREAM</span></h1><pre><code class="cpp">#include &lt;iostream&gt;
using namespace std;
...
cin &gt;&gt; a;
cout &lt;&lt; b;</code></pre>
<p>上面这一段应该是大多数人最早接触的 cpp 代码。</p>
<p>大概讲解一下这一段内容中每行代码发生了什么。</p>
<p><code>#include &lt;iostream&gt;</code> 引入 iostream 头文件</p>
<p><code>using namespace std;</code> 使用 std 命名空间，命名空间如果有时间放在后面讲。<font color="#E0E0E0">反正也没人用得到不如不讲了。</font></p>
<p>关于 cin 和 cout，我们可以构造这样一个模型：</p>
<pre class="mermaid">graph LR;
    input-->cin;
    cin-->variable;
    variable-->cout;
    cout-->output;
    style cin fill:#CFC,stroke:#333,stroke-width:0.5px
    style cout fill:#CFC,stroke:#333,stroke-width:0.5px
    style variable fill:#FFC,stroke:#333,stroke-width:0.5px</pre>

<p>cin 和 cout 仅仅是一个管道，用来让数据从中流过（data stream），这就是所谓的 iostream（input &amp; output stream）。</p>
<p><code>cin</code> 可以用来输入多个值，如 <code>cin &gt;&gt; a &gt;&gt; b;</code></p>
<p><code>cin</code> 可以隐式转换为 <code>bool</code> 。</p>
<p><code>cout</code> 用法同理。</p>
<hr>
<h1><a href="#xin-de-guan-jian-zi" class="header-anchor"># </a><span id="xin-de-guan-jian-zi">新的关键字</span></h1><p>本篇只介绍 <code>new</code> 和 <code>delete</code> 。</p>
<h2><a href="#jian-dan-jie-shao-malloc-xi-lie" class="header-anchor"># </a><span id="jian-dan-jie-shao-malloc-xi-lie">简单介绍<code>malloc</code> 系列</span></h2><p><code>malloc</code> 及其内存分配系列函数（如<code>calloc</code> ）均在 stdlib.h 中，而 <code>new</code> 和<code>delete</code> 是语言本身的内存分配机制。</p>
<p><code>malloc</code> 需要一个参数，<code>void * malloc(size_t _NumOfBytes)</code> ，这个参数表示所需空间的大小，并且不对这段空间进行清空，用法如<code>int *arr = (int *)malloc(n * sizeof(int));</code> 表示分配 n 个 int 的空间。</p>
<p>其衍生函数，<code>calloc</code> ，需要两个参数，声明形式如<code>void * calloc(int _NumOfElements, size_t SizePerElement)</code> ，用法如<code>int *arr = (int *)calloc(n, sizeof(int));</code> ，需要注意，这个函数分配后会将这一段内存置为 0。</p>
<h2><a href="#new-he-delete-yong-fa" class="header-anchor"># </a><span id="new-he-delete-yong-fa"><code>new</code> 和<code>delete</code> 用法</span></h2><p><code>new</code> 用法示例如下：</p>
<pre><code class="cpp">int *a = new int; // 表示让 a 指向分配的 int 类型值，值未初始化
&quot;上面这一行等价于&quot;;
int *a = (int*)malloc(sizeof(int));

int *b = new int(); // 表示让 b 指向分配的 int 类型值，并在分配后采用默认方式初始化这个值
// 对于 int 类型，默认方式初始化意味着置为 0

int *c = new int(3); // 表示让 c 指向分配的 int 类型值，并在分配后初始化这个值为 3
// *c == 3

int *d = new int[n]; // 表示让 d 指向分配的长度为 n 的 int 数组，内存未初始化
&quot;上面这一行等价于&quot;;
int *d = (int*)malloc(n*sizeof(int));

int *e = new int[n](); // 表示让 e 指向分配的长度为 n 的 int 数组，并采用默认方式初始化每个值
// e[0] == e[1] == e[2] == ... == e[n-1] == 0

// 特别的，有些类型分配的时候略微麻烦一点（类型系统的进阶使用）
int **a= new (int *[n]); // 长度为 n 的 int* 数组
int (**pf)()= new (int (*)()); // pf 为指向函数指针的指针，指向的函数 f 声明形式如 int f(void);
int (**pfs)()= new (int (*[n])()); // pfs 为函数指针数组</code></pre>
<p><strong>无法用 new 在创建一个数组的同时初始化每个元素</strong></p>
<hr>
<p><code>delete</code> 用法类似。（采用<code>new</code> 举例中所用的变量）</p>
<pre><code class="cpp">delete a;

delete b;

delete c;

delete []d; // 删除数组

delete []e;</code></pre>
<hr>
<h2><a href="#new-de-si-kao" class="header-anchor"># </a><span id="new-de-si-kao"><code>new</code> 的思考</span></h2><p>能否用<code>new</code> 在创建一个数组后初始化每个元素呢？<font color="white"> placement new </font></p>
<h1><a href="#han-shu-duo-tai-chong-zai-yu-mo-ren-can-shu" class="header-anchor"># </a><span id="han-shu-duo-tai-chong-zai-yu-mo-ren-can-shu">函数多态（重载）与默认参数</span></h1><h2><a href="#han-shu-duo-tai" class="header-anchor"># </a><span id="han-shu-duo-tai">函数多态</span></h2><p>例如，我们有</p>
<pre><code class="cpp">int max(int a, int b) { return a&gt;b?a:b;}</code></pre>
<p>如果我们想要三个数比较该怎么做？</p>
<p>在 c 中，</p>
<pre><code class="c">int max3i(int a, int b, int c) { int t = max(a, b); return t&gt;c?t:c;}</code></pre>
<p>而在 cpp 中，我们可以直接使用</p>
<pre><code class="cpp">int max(int a, int b, int c) { int t = max(a, b); return t&gt;c?t:c;}</code></pre>
<p>cpp 可以根据参数的数量和类型来自动判断调用哪一个函数。</p>
<p>以至于我们可以</p>
<pre><code class="cpp">int max(int a) { int t = a; return t&gt;a?t:a;}
int max(int a, int b) { int t = max(b); return t&gt;a?t:a;}
int max(int a, int b, int c) { int t = max(b, c); return t&gt;a?t:a;}
int max(int a, int b, int c, int d) { int t = max(b, c, d); return t&gt;a?t:a;}
int max(int a, int b, int c, int d, int e) { int t = max(b, c, d, e); return t&gt;a?t:a;}
int max(int a, int b, int c, int d, int e, int f) { int t = max(b, c, d, e, f); return t&gt;a?t:a;}</code></pre>
<p>这时候我们可以传入 1-6 个参数来最大值，并且除了单个参数的情况，其他具有一致性。</p>
<p>再例如，常见的错误代码 <code>int a = pow(5, 2)</code> ，我们可以定义 pow 的多态：</p>
<pre><code class="cpp">int pow(int a, int n)
{
    if(n&lt;0) return 0;
    int res = 1;
    for(int i=0; i&lt;n; ++i) res *= a;
    return a;
}</code></pre>
<p>可以避免浮点误差。</p>
<hr>
<h2><a href="#mo-ren-can-shu" class="header-anchor"># </a><span id="mo-ren-can-shu">默认参数</span></h2><p>上面介绍了函数多态，这里介绍函数默认参数。</p>
<p>例如我们需要一个求和函数，我们预定有四个数字要被传入。</p>
<pre><code class="cpp">int sum(int a, int b, int c, int d) { return a+b+c+d;}</code></pre>
<p>但是随着时间流逝，我们有时候需要传入三个参数。</p>
<pre><code class="cpp">int sum(int a, int b, int c) { return sum(a, b, c, 0);}</code></pre>
<p>以及两个······</p>
<pre><code class="cpp">int sum(int a, int b) { return sum(a, b, 0);}</code></pre>
<p>甚至一个············</p>
<pre><code class="cpp">int sum(int a) { return sum(a, 0);}</code></pre>
<p>甚至没有参数<del>（👈这样的函数有意义吗？？？）</del></p>
<pre><code class="cpp">int sum(void) { return sum(0);}</code></pre>
<p>这里的求和函数和上面的 max 形式很像，并且一路规约下来也很自然，只是略微冗杂了点。</p>
<p>能不能更简单呢？</p>
<p><font size="7">能！</font></p>
<p>cpp 允许函数含有默认参数。</p>
<p>上面的 <code>sum</code> 系列函数使用默认参数后只需要一个函数即可。</p>
<pre><code class="cpp">int sum(int a=0, int b=0, int c=0, int d=0) { return a+b+c+d;}</code></pre>
<p>这个函数的定义表示，如果没有传入对应参数，则对应参数默认置为 0。</p>
<p>如 <code>sum(1,2,3)</code> ，其中各参数即为<code>a=1, b=2, c=3, d=0</code> 。</p>
<p>在 cpp 中，各参数只能从左向右排布，因此默认参数需要从右向左赋值。</p>
<p>函数的默认参数只能在声明或定义中出现一次。</p>
<hr>
<h1><a href="#xin-de-lei-xing" class="header-anchor"># </a><span id="xin-de-lei-xing">新的类型</span></h1><h2><a href="#yin-yong" class="header-anchor"># </a><span id="yin-yong">引用</span></h2><p>引用类型是值和指针的折中。</p>
<p>引用的主要用途是函数传参和创建别名。</p>
<p>函数传递较大的数据类型由于需要整个拷贝，因此在 c 中通常传递指针作为参数，例如：</p>
<pre><code class="c">typedef struct data
{
    int a[1000000];
    int b[1000000];
}data, *pdate;

int f(pdata p)
{
    pdata-&gt;a ...
    pdata-&gt;b ...
}</code></pre>
<p>但是频繁的指针操作并不优雅，因此我们使用引用。</p>
<pre><code class="cpp">int f(data&amp; d)
{
    d.a ...
    d.b ...
}</code></pre>
<p>在这里我们可以像使用一个 data 类型一样使用 data&amp; 类型。</p>
<p>引用和其实体共享一个对象，例如</p>
<pre><code class="cpp">int a = 2;
int &amp;b = a;
b = 3;</code></pre>
<p>此时 a 也被改变。</p>
<h2><a href="#class" class="header-anchor"># </a><span id="class">class</span></h2><p>类是类似结构体的数据类型。</p>
<pre><code class="cpp">struct A
{
    int a;
} a;

class B
{
    int b;
} b;

int main()
{
    printf(&quot;%d\n&quot;, a.a);
    printf(&quot;%d\n&quot;, b.b);
}</code></pre>
<p>类的内容默认对外不开放。</p>
<p>类内可以定义函数（方法）。</p>
<pre><code class="cpp">class B
{
public:
    int print();
private:
    int b;
} b;</code></pre>
<p>public 表示类外可以访问，private 表示类外无法访问。</p>
<p>这时候我们可以使用 b.print() 来输出，但是不能修改 b.b 的内容。 </p>
<p>特别的，cpp 中的 struct 也不同于 c 中的，它更像是被修改了默认访问权限的 class。</p>
<hr>
<h1><a href="#nei-lian-yu-hong" class="header-anchor"># </a><span id="nei-lian-yu-hong">内联与宏</span></h1><h2><a href="#nei-lian-de-hao-chu" class="header-anchor"># </a><span id="nei-lian-de-hao-chu">内联的好处</span></h2><p>宏是一种功能强大的预处理工具。</p>
<p>但是含有隐患。</p>
<pre><code class="c">#define mult(a, b) a*b</code></pre>
<p>如果使用 </p>
<pre><code class="c">int x = mult(3+2,2+3);</code></pre>
<p>如常理所想应该是 x 被赋值为 (3+2)*(2+3) == 25，但是实际上，这句在展开后会成为</p>
<pre><code class="c">int x = 3+2*2+3; // x == 10</code></pre>
<p>诚然，我们可以采用加括号的方式来封闭宏，但是总有力所不逮之处（为什么不行？思考题）。</p>
<p>因此，cpp 创建了内联的语法。</p>
<pre><code>inline int mult(int a, int b) { return a*b;}
int x = mult(3+2,2+3);</code></pre><p>这时候，第二行<code>int x = mult(3+2,2+3);</code> 就相当于展开成这样的三行：</p>
<pre><code class="cpp">int a = 3+2;
int b = 2+3;
int x = a*b;</code></pre>
<p>可以确保其正确性。</p>
<h2><a href="#wei-shi-me-huan-yao-hong" class="header-anchor"># </a><span id="wei-shi-me-huan-yao-hong">为什么还要宏？</span></h2><p>内联如此方便、强大、安全，为什么要宏呢？</p>
<p>这里是宏的一些（内联做不到的）用法：</p>
<pre><code class="cpp">#include &lt;cstdio&gt;

int test_function(void);

int main()
{
    test_function();
    printf(&quot;%s\n&quot;, __FUNCTION__);
}

int test_function(void)
{
    printf(&quot;%s\n&quot;, __FUNCTION__);
}</code></pre>
<hr>
<p>例如上面的 <code>mult</code> ，也可以使用宏的方式来正确处理。</p>
<pre><code class="cpp">#define mult(a, b) ((a)*(b))</code></pre>
<p>但是，它在维持原有参数结构的情况下，不能做到和内联一样安全。</p>
]]></content>
      
        
        <tags>
            
            <tag> 社团课程 </tag>
            
        </tags>
        
    </entry>
    
    <entry>
      <title><![CDATA[看板娘配置]]></title>
      <url>/2019/10/20/%E7%9C%8B%E6%9D%BF%E5%A8%98%E9%85%8D%E7%BD%AE/</url>
      <content type="html"><![CDATA[<h3><a href="#kan-ban-niang-pei-zhi" class="header-anchor"># </a><span id="kan-ban-niang-pei-zhi">看板娘配置</span></h3><p>参考网上教程配置了 Live2D 看板娘。</p>
<a id="more"></a>

<h2><a href="#an-zhuang" class="header-anchor"># </a><span id="an-zhuang">安装</span></h2><h3><a href="#an-zhuang-kuang-jia" class="header-anchor"># </a><span id="an-zhuang-kuang-jia">安装框架</span></h3><p>根目录下，执行</p>
<pre><code class="bash">npm install --save hexo-helper-live2d</code></pre>
<p>此后在根目录下 __config.yml 中新增：</p>
<pre><code class="bash"># Live2D
## https://github.com/EYHN/hexo-helper-live2d
live2d:
  enable: true
  # enable: false
  scriptFrom: local # 默认
  pluginRootPath: live2dw/ # 插件在站点上的根目录(相对路径)
  pluginJsPath: lib/ # 脚本文件相对与插件根目录路径
  pluginModelPath: assets/ # 模型文件相对与插件根目录路径
  # scriptFrom: jsdelivr # jsdelivr CDN
  # scriptFrom: unpkg # unpkg CDN
  # scriptFrom: https://cdn.jsdelivr.net/npm/live2d-widget@3.x/lib/L2Dwidget.min.js # 你的自定义 url
  tagMode: false # 标签模式, 是否仅替换 live2d tag标签而非插入到所有页面中
  debug: false # 调试, 是否在控制台输出日志
  model:
    use: live2d-widget-model-wanko # npm-module package name
    # use: wanko # 博客根目录/live2d_models/ 下的目录名
    #use: ./source/live2d/wanko # 相对于博客根目录的路径
    # use: https://cdn.jsdelivr.net/npm/live2d-widget-model-wanko@1.0.5/assets/wanko.model.json # 你的自定义 url
  display:
    position: right
    width: 150
    height: 300
  mobile:
    show: false # 手机中是否展示
    scale: 0.5
</code></pre>
<h3><a href="#an-zhuang-mo-xing" class="header-anchor"># </a><span id="an-zhuang-mo-xing">安装模型</span></h3><p>安装模型有两种方式，第一种是通过 npm 下载，如：</p>
<pre><code class="bash">npm install live2d-widget-model-wanko</code></pre>
<p>另一种方式是自行下载包，安装到任意位置，此后在 __config.yml 中 live2d-&gt;model 写明适当位置即可。</p>
<hr>
<h1><a href="#xie-zai" class="header-anchor"># </a><span id="xie-zai">卸载</span></h1><p>根目录下执行</p>
<pre><code class="bash">npm uninstall hexo-helper-live2d</code></pre>
<hr>
<p><em>感谢 <a href="https://sevencho.github.io/" target="_blank" rel="noopener">曹雪松de博客|CoderBoy’s Blog</a></em></p>
]]></content>
      
        
        <tags>
            
            <tag> 维护 </tag>
            
        </tags>
        
    </entry>
    
    <entry>
      <title><![CDATA[站点搭建过程]]></title>
      <url>/2019/10/20/%E6%90%AD%E5%BB%BA%E8%BF%87%E7%A8%8B/</url>
      <content type="html"><![CDATA[<h1><a href="#zhan-dian-da-jian-guo-cheng" class="header-anchor"># </a><span id="zhan-dian-da-jian-guo-cheng">站点搭建过程</span></h1><p>基于 hexo 搭建了自己的站点，在此发布一下搭建教程。</p>
<a id="more"></a>

<h2><a href="#yi-lai" class="header-anchor"># </a><span id="yi-lai">依赖</span></h2><ul>
<li>配置 node.js</li>
<li>配置 hexo</li>
</ul>
<h2><a href="#ding-zhi" class="header-anchor"># </a><span id="ding-zhi">定制</span></h2><ul>
<li>设置 theme</li>
</ul>
<h2><a href="#ming-ling-xing-cao-zuo" class="header-anchor"># </a><span id="ming-ling-xing-cao-zuo">命令行操作</span></h2><ol>
<li><p><code>hexo new &lt;title&gt;</code></p>
<p>创建新文章</p>
</li>
<li><p><code>hexo g</code></p>
<p>构建静态页面(<code>hexo generate</code> )</p>
</li>
<li><p><code>hexo clean</code></p>
<p>清除已构建的页面</p>
</li>
<li><p><code>hexo s</code></p>
<p>本地服务器(<code>hexo server</code>)</p>
</li>
<li><p><code>hexo d</code></p>
<p>发布、部署(<code>hexo deploy</code>)</p>
</li>
</ol>
]]></content>
      
        
        <tags>
            
            <tag> 维护 </tag>
            
        </tags>
        
    </entry>
    
  
  
</search>
